# 종운 그룹웨어 — 기능 대장 (사용자 매뉴얼 원천)

> 살아있는 문서. 기능 추가·변경 시 이 파일을 함께 갱신한다. 기준: v327 (2026-09-08).
> 세부 변경 이력은 git 커밋 메시지(한글) 참조.

## 1. 전체 구조

- **홈(대시보드, 시안 B)**: KPI 타일(진행 계약·금액/낙찰 대기/미수·45일↑/이번 주 기한·만기/결재) + **파이프라인 보드**(수주→계약·서류→시공 중→기성·수금 4열 — 낙찰 카드에 [계약 시작], 지연·45일 미수는 붉은 테두리, 클릭 시 해당 탭) + **오늘 흐름 피드**(수집·개찰·정기업무·수금 자동 기록). 모바일 2×2열
- **관리**: 지시 / 기성·수금 / 거래처 / 인사·직원(관리자) / 인사·일용직(권한 설정) / 진척도(관리자)
- **수주**: 입찰(관리자) / 계약 / **견적서** / 부동산(온비드, 관리자)
- **업무**: 캘린더 / 차량 / 인허가 / 문서함
- **개인**: 내 업무(내게 온 지시·내 할 일·주기 체크) / 휴가

권한: 회원별 모듈 권한(숨김/보기/처리) — 인사·직원 탭 권한관리에서 설정. 서버가 강제(클라이언트 우회 불가). 감사로그 자동 기록.

## 2. 계약 파이프라인 (핵심)

**시나리오 조립**: 계약 시작 시 요소 체크(석면철거·노동부신고·일반철거·대형철거·폐기물·준설·적격심사) → 단계 자동 조립. 발주처 종류와 무관하게 요소가 단계를 결정. 마지막은 항상 `청구 → 수금확인`.

**단계 = 지시**: 현재 단계가 지시 탭에 할 일로 발행. 완료(승인) 시 다음 단계 자동 생성. 지시의 [📄 서류] 버튼이 그 단계 서류를 바로 작성(단계가 서류를 이끈다).

**두 회사 규칙**: 철거·석면=㈜종운건설 / 폐기물·준설=(유)종운환경 / 철거·석면 안의 폐기물 신고·운반 서류=종운환경. 서류 생성 시 자동 선택(수동 변경 가능).

**입찰 연동**: 입찰 탭 낙찰 처리 → 계약 전환 시 공고명·기관·낙찰금액 자동 유입. 되돌리기(↩)는 재확인 후 직전 단계로. **개찰봇이 낙찰을 감지한 경우에도 접속 시 계약 시작 창 자동 오픈**(계약 미연결 건, 세션당 1회 — 닫으면 배너·카드 버튼으로). 계약 생성 시점에 개찰결과가 없어 금액이 비었더라도 **서류 생성·기성 등록·실적·계산기에서 연동 공고의 낙찰금액으로 자동 폴백**(`conAmt`). [대장](건축물대장 조회)은 **석면·철거 등 건축물 있는 계약에만 노출**(준설·폐기물 단독 건 숨김). 계약서류의 **계약보증금률 자동 프리필**: 관급 입찰 건 10%(지방계약법령 기준)·그 외 5%(포항시 수의 양식 실측) — 공고·계약서와 다르면 수정.

## 2.5 견적서 탭 (문서번호 관리)

계약 파이프라인과 **별개로** 발행하는 견적서 전용 탭. 계약이 아직 없는 건도 발행 가능.

- **견적번호 자동 채번**: `YYYYMM-NN` — 해당 월 발행분 중 최대 일련 +1 (수동 수정 가능, 중복 시 경고). 일자를 바꾸면 그 달 기준으로 다시 채번
- **발행 이력**: 번호·일자·수신처·용역명·금액·시행회사 목록(번호 역순). 내부 메모 칸 별도
- 행별 동작: **hwpx 재발행**(언제든 다시 내려받기 — 회사별 양식 자동 선택) / 수정 / **계약 생성**(승격 — 회사에 맞는 유형·요소로 계약 시작, 견적에 계약 연결 칩 표시) / 삭제(숨김 — 시점 복구 가능)
- 금액=수량×단가, 한글금액 자동. 저장은 quotes 컬렉션(다른 컬렉션과 동일한 병합·충돌 재시도 방식)
- **권한**(v238): 견적서는 독립 권한 모듈 — **기본 숨김**(영업 직렬에게만 명시 부여, 서버·탭 양쪽 강제). 계약 파이프라인의 견적 "서류" 생성(수의 요소)은 별개로 계약 권한을 따름

## 2.7 홍보 탭 (현장 기록 → 블로그 초안, v240)

현장 사진+정보 1건 입력 → 블로그 초안 자동 생성 → 관리자 검수 → 게시 추적. 유사문서 억제 사고(블로그 44건, 2026-08-05 실측)의 재발 방지가 핵심 사양.

- **입력**(운영 담당 — 권민지 전담): 사진(최대 20장, 1600px 리사이즈 후 서버 첨부 kind:promo) + 지역·시설·문제·수치 4칸
- **GPS 자동 지역**: JPEG EXIF에서 GPS 추출(자체 파서, 라이브러리 없음) → 서버 promo_geo(OSM Nominatim 역지오코딩)로 동 단위 지역명 자동 기입. **원본 화질 전송본만 GPS 생존**(실측: 일반 전송은 EXIF 전멸 — Galaxy S26 확인, 아이폰·구버전 안드로이드는 기종별 검증 대기). 없으면 수동 폴백
- **변주 엔진**: 제목 공식 5종 × 본문 틀 4종을 직전 기록과 다른 조합으로 강제 회전(블로그_작성지침_v1 내장). 보드 하단에 변주 상태 상시 표시
- **보드**(대시보드 B와 같은 4열 문법): 검수 대기 → (반려) → 게시 대기 → 게시 완료. 카드 클릭=상세
- **검수 게이트**: 승인·반려(사유)는 관리자만. 승인 후 [복사] → 네이버 블로그 붙여넣기(외부 글쓰기 공식 API 없음 — 반자동이 사양) → [게시 완료로 표시]
- **권한**: promo 독립 모듈, **기본 숨김**(quote와 동일 — 권민지·관리자에게만 명시 부여). 저장은 promo 컬렉션(병합·충돌 재시도 동일)
- **사진AI 자동 실행(v261, 2026-08-14 PM 확정 "사진 올리기→발행 클릭 두 동작")**: 기록 저장이 서버에 실린 직후 gw-promo-ai(pa_generate)를 버튼 없이 자동 기동 → 결과가 오면 기록의 제목·본문에 자동 적용(변형 생성기 초안은 pre_ai로 보존, 검수 화면 [되돌리기]로 복구). 검수 화면을 열어 둔 경우엔 입력칸에만 채움(기존 v260 동작). 자동 실행 실패는 무음 — 예비 초안(변형 생성기)이 이미 있고 버튼으로 재시도. 수동 [사진으로 다시 쓰기] 버튼은 유지(재생성용)
- 후속 킵: 사진대지(hwpx) 생성 연결, 기종별 GPS 검증
- **홈페이지 갤러리 동시 게시(혁신⑥, v291)**: [블로그 게시 완료로 표시]에서 글 주소 입력 → 서버(gw-gallery-relay)가 저장된 posted+blog_url 기록을 검증해 홈페이지 data/gallery.json + 대표 사진 3장을 Git Trees 원자 커밋 → gw-gallery/ 브랜치 → staging PR(자동 머지 없음, 머지는 PM). 재시도 버튼 [홈페이지 갤러리 전송], 중복은 서버 차단(gallery:pr blob + 동일 URL 검사)
- **포항 시공 지도 데이터 내보내기(혁신⑦, v292)**: 게시 완료(posted) 기록 중 GPS 자동 축적분(EXIF gps 필드)을 서버(gw-map-relay)가 동·읍 단위로 접어 홈페이지 data/map.json 전량 재빌드 → gw-map/ 브랜치 → staging PR. 좌표·번지·발주처·기록 id는 내보내지 않음(동·읍+공종+연월+시설요약+블로그URL만). 트리거: 게시 완료 저장·[홈페이지 갤러리 전송] 시 무음 자동, 내용 동일하면 PR 생략. 홈페이지 쪽 페이지(map.html·js/map.js, 정적 SVG A안)는 website 레포 브랜치 gw-map-7에 대기
- **완성본 한 장(v262, 2026-08-14 PM 발상)**: 검수 화면 **[🖼 완성본 만들기]** → 본문의 `[사진 n~m : …]` 마커 자리에 실제 사진(att_get→data URI)을 끼운 완성 글이 새 창에 뜬다. **[전체 복사]**(리치 HTML) → 네이버 글쓰기에 Ctrl+V 한 번이면 글+사진이 자리까지 맞은 채로 들어간다. **[문서로 저장(.doc)]**로 워드·한글에서 열 수도 있음. 마커에 안 잡힌 사진은 뒤에 붙여 누락 방지. 네이버를 프로그램이 조작하지 않음 — 사람이 페이지를 복사해 사람이 붙여넣는 통상 사용이라 계정 위험 0. ※ 에디터가 다중 이미지 붙여넣기를 어떻게 받는지는 실사용 1회로 확정 필요
- ~~네이버 게시 = 플랫폼이 구동하는 게시 도우미(A안)~~ **폐기 확정(v267, 2026-08-15 PM "도우미 걍 폐기")** — 완성본 한 장 복붙 경로가 대체. Electron 실코드 백업은 `C:\Users\user\jongwoon\.backups\jw-blog-helper-code-20260818\`(폴더 본체 556MB는 8/18 삭제). 아래는 판정 기록:: 서버(Netlify) 자동 주입은 불가 — 네이버 글쓰기에 봇탐지(wtm)+캡차(nCaptcha)가 걸려 있고(PM HAR 실측) Netlify는 데이터센터 IP라 더 잘 막힌다. 화관법(정부사이트·ID/PW·봇탐지 없음)과 다름. **웹앱은 교차출처·file input 보안으로 네이버를 직접 조작 못 함** → 브라우저 밖 네이티브 조각 불가피했음. 세션 재생·API 뜯기·캡차 우회는 안 함(UI 자동화만 검토). 운영자=홍보 담당, PM은 루프 밖(앱 검수만)
- **블로그AI 실전화(v263~v281, 2026-08-14~16 — 실물 4회 왕복으로 확정)**: ①마커 파서 관용(v270·272 — 쉼표 나열·대괄호 생략·마커 뒤 줄 공유·전각 콜론·"3과 4"·공백 나열 전부 인식, 앱 promoMarkNums↔서버 markerNums **자구 동률을 uismoke가 강제**). ②사진 상한 30장 — 화면·라이브러리·워커·**디스패처** 4파일 사슬(디스패처 누락이 "30장 올려도 13장" 실사고 진범, uismoke 검사). ③공개 주소(gw-promo-img, 24h 토큰)/내장 이미지 이중 복사 경로 + 고아 사진은 마지막 사진 뒤 이어붙임·경고문은 복사에서 제외(v274). ④프롬프트를 외부 실측으로 재교정(v275~277 — 네이버 공식 원문 11건·상위 노출 동종업체 6편 전수 파싱): 장면 순서(상태 확인→원인→안전조치→본작업→전후 비교), **사진 1~2장씩 교차**(3장+ 묶음 금지), 사진 밑 서술형 캡션(마커 설명이 캡션으로 렌더), 본문 길이 사진 연동(bodyBounds: 장당 250~350자, ≤7장은 2,000~2,600자). ⑤분량은 글자 수 지시가 아니라 **구조 할당량**으로 강제(v280 — 덩어리 n÷2.5개·소제목 n÷3.5개·사진당 3문장, 사진 8장+는 effort high, 숫자는 koNum/koCount 한글 수사 — 계약금액 차단 충돌 방지). ⑥AI 초안 도착 즉시 기록 저장(v273 — 새로고침 증발·재생성 과금 방지, [되돌리기]도 저장까지). ⑦검수 경고: 번호 누락·중복·3장+ 묶음·작업 순서 역행(사물/장소/전후 낱말 오탐 2회 수리)·분량(사진 기준). ⑧생성 타임아웃 10분·타임아웃 재시도 금지·MAX_TOKENS 24000·폴링 12분(v281). ⑨태그 15~20개 생성+[태그 복사], 명함 해체 꼬리(석면·철거→건설, 그 외→환경 자동 판별. **PROMO_TEL 비어 있음 — 전화번호 확정 대기**). 현재 관문: PM 재생성 테스트(합격=4,500자+·고아 0·경고 0) → **첫 실게시**(0건)

## 2.8 블로그 제목 원형 다양화 (v320, 2026-09-06)

앱 AI 제목 22/22가 `포항 ○○동 {시설} {증상}, {훅}` 한 골격으로 굳은 실측(9/4 조사 `09_조사_문서/블로그제목_다양화_20260904.md`)의 수리. 유사문서 억제(8/11 전수조사: 제목 앞부분 동일 → 잘림·클릭 0)의 제목 쪽 마무리. 원인 3(회전 미강제·재생성 시 자기 제목 못 봄·검수 경고 2종뿐)을 각각 막는다.

- **원형 11종**(코드는 스키마 enum·잡 blob·이력 라벨에 그대로 쓰이므로 바꾸지 말 것): Q 질문형 / N 수치·기록형(담당자 수치 또는 사진 8장+) / C 비교·대비형 / P 과정·순서형 / O 현장 관찰형 / T 기간·주기형(비용형의 대체 — 금액은 계약 차단상 불가) / M 실수·오해 예방형 / S 계절·시기형(촬영일 필수) / L 지역·장소형(동·리 단위 + 지역명 선두 허용 시) / K 판단 신호형 / R 관리 주체·절차형. 정의·규칙·예시 2개씩은 `_lib/promoai.js` TITLE_TYPES(예시는 user 메시지에만 실림, 4자리 숫자 금지 — 금액 차단 함정).
- **선택 규칙**(`pickTitleType`, 순수 함수·난수 없음): 제외 = 최근 게시 3건의 원형 ∪ 이 기록의 이전 제목(현재·예비 생성기·이력 blob) 원형 → 조건 필터(N·S·L) → 후보<2면 단계 완화(자기 기록 해제 — 직전 회차 원형 1건은 유지 → 직전 1건만 → 전체, `relaxed` 기록 + 검수 경고 '제목 원형 후보가 모자라 제외를 완화했습니다') → FNV-1a(기록 id + ':' + 재생성 회차) mod 후보 수 = 주 원형, 해시 상위 비트 = 보조. 같은 기록·같은 회차면 같은 선택, 회차가 바뀌면 다른 선택. 최근 3건 중 2건 이상이 지역명 선두면 L 제외 + "지역명을 첫 어절에 두지 마십시오". 모델에는 주(규칙+예시 2)·보조(규칙+예시 1)·제외 목록·직전 골격·자기 이전 제목 블록을 user 메시지로 주고, 출력 스키마 `title_type`(enum 11)로 실제 사용 원형을 받는다.
- **라벨 영속(핵심 — 9/4 반박 검증이 지적한 결함의 해소)**: 워커가 done 때 잡 blob `promoai:job:<job>`(`title_type`·`title_plan{primary,backup,exclude,lead_ok,relaxed,labels}`)과 **이력 blob `promoai:hist:<기록id>`**(`{items:[{title,tt,ts,job}] 최근 12건, n 회차, updated_at}`)에 기록. 다음 생성의 `recentTitleInfo`(라이브러리 순수 함수)는 후보 5건(`recentTitleCandidates` — 유효 시각 max(게시일, AI 생성 시각 `p.ai.ts`) 내림차순, 상태 순위 없음·삭제/반려만 제외, 게시도 AI 초안도 없는 기록(예비 제목 그대로)은 후보 밖: 9/6 2차)마다 (a) 이력 blob에서 현재 제목과 같은 항목의 tt(정확 → 느슨 대조 `titleMatch`: 지역명 뺀 어절 4개+·70%+) → (b) `p.ai.tt`(index.html이 저장) → (c) 제목 추정 순으로 읽는다. **index.html을 안 고쳐도 (a)로 라벨이 흐른다.** 추정은 규칙 1개 단독 일치(high)만 원형 제외에 쓰고, 경합(low)·불명(none)은 원형을 제외하지 않고 골격(접두·지역명 위치·종결·구두점)만 제외한다(틀린 추정으로 엉뚱한 원형을 막지 않기 위해). 워커 로그 `[제목] 최근 n건(라벨 이력 a·기록 b·추정 c) · 이 기록 이전 제목 m건 · 회차 k`.
- **경고 18종**(`titleWarnings` → draftWarnings 끝에 합류(마커·장면 경고 뒤) → 워커 갈래별 상한(사진·본문 12 + 제목 8) → 검수 화면 점검 줄): 50자 초과·상호·줄바꿈(복붙 경계 치명)·느낌표·이모지·대괄호/마크다운·4자리 숫자·구 템플릿 상투구·**구두점 2개 이상·계절어(촬영일 없음)·완화 발생**(9/6 2차)·첫 두 어절 동일·어절 3개 겹침(시·군·읍·동 낱말과 region 토큰의 면·리 제외)·직전 글 골격 동일·원형 불명 글과 골격 겹침·자기 이전 제목과 완전 동일·지역명 선두 위반·원형 불일치/미기록. 실패가 아니라 검수 표시.
- **화면(index.html, ES5)**: 결과 `tt`를 paResults와 `p.ai.tt`에 저장, 상태 문구에 "제목 원형 M 실수·오해 예방형"(PA_TITLE_TYPE_NAMES — 코드 11종 라이브러리와 동률, uismoke 7b). **검수 문구**: 점검 경고가 '제목…'뿐이면 "재생성 대신 제목 칸만 고쳐 저장하면 됩니다(첫 두 어절 바꾸기 / 겹친 낱말 하나 바꾸기 / 쉼표·대시 자리 바꾸기)". 사람이 제목을 바꿔 저장하면 `p.ai.tt`를 지운다(라벨은 AI가 지은 그 제목에만 유효 — 설계 §6-3의 "지우지 않는다"를 뒤집음: 지우지 않으면 서버가 낡은 라벨을 믿는다). 예비 생성기(PROMO_TITLES)·마커 정규식·사진 상한 무변경.
- **측정 지표**(배포 후, 게시 없이 잡 blob만으로): ① `promoai:job:*`의 `title_type` 분포 — 10건 연속에 같은 원형 3회 이하 ② `warn` 중 '제목' 경고율 ③ 지역명 선두 비율 50% 이하 ④ `title_plan.relaxed` 발생 0(발생하면 검수 경고 '제목 원형 후보가 모자라…'로 화면에도 보임) ⑤ 워커 로그의 '추정' 건수가 게시가 쌓일수록 0에 수렴(라벨 영속 확인). 9/18 검색 노출 판정과 별개.
- **회귀 시험**: `05_홍보_검색노출/gen_test/title_test.js`(리포 `_lib/promoai.js`를 직접 require) — ⑧ 1단계 조건 시뮬레이션(라벨 없는 과거 5건 + 이후 생성분은 이력 blob 라벨, 라벨 심기 없음) 30회에서 직전 3건 원형 반복 0, 1판 설계(p.ai.tt만) 재현 시 반복 발생. `tools/servertest.mjs` 25절: 워커를 인메모리 blob으로 돌려 잡·이력 blob 기록과 (a)(b)(c) 라벨 경로·재생성 회차·계약 금지 문자열 이력 제외를 실측. `tools/uismoke.mjs` 7b: 라이브러리 exports ↔ 워커 필수 목록 ↔ 이력 키 ↔ 화면 tt 저장 사슬.
- **9/6 2차 검증 반영**(반박 검증 확정 결함 9건, 로컬 커밋 "fix: 블로그 제목 검증 반영", sw v320 유지·index.html 무변경): ① 최근 제목 후보 정렬을 '게시 우선'에서 유효 시각(max(게시일, `p.ai.ts`)) 내림차순으로, 게시도 AI 초안도 없는 갓 등록 기록은 후보 밖(ts로 넣으면 예비 제목이 실제 이력을 밀어낸다 — servertest 실측) — 같은 날 기록 여러 건을 등록해 초안이 게시 없이 쌓여도 초안끼리 서로를 본다(라이브러리 시뮬레이션 `title_test.js` ⑧I, dfe06ba 대비 재실측: 일괄 초안 3/5/30건 직전 3건 반복 6·11·17/30 → 0/30) ② 검수 경고 상한을 갈래별로(사진·본문 12 + 제목 8) + 제목 경고를 마커·장면 검사 뒤에 합류 — 제목 경고가 사진 연동 경고를 밀어내지 않는다 ③ `titleLeadsWithRegion`·`titleWords`의 면·리 접미는 region 토큰과 일치할 때만 지역명(반나절이면·놔두면·마무리 오판 수리), 최근 제목은 그 기록의 `region`(후보·recent_titles에 동반)으로 판정 ④ 원형 예시가 계약 금지 문자열과 겹치면 그 예시만 빼고 조립(`ex_skip` 라벨, 예시 전부 빠진 원형은 후보 제외), R 예시의 '포항 초등학교' 제거, 실패 회차(이력 blob `fail_n`)를 씨앗에 더해 같은 계획으로 영구 차단되지 않게 ⑤ 완화 1·2단계에서도 직전 회차 원형 1건은 계속 제외(재생성 16회 × 20 seed 직전 회차 동일 16/320 → 0/320) ⑥ 완화 발생 시 '제목 원형 후보가 모자라…' 경고 ⑦ Q·N·C·O·M 예시의 계절어 제거 + 촬영일 없는 기록의 제목 계절어 경고 ⑧ 예시 구두점을 규칙(쉼표·대시 합쳐 하나)에 맞추고 Q 규칙을 '질문 앞세우고 지역명은 뒤에 쉼표'로, `titleEnding`이 "…일까요, 지역명" 골격을 질문 종결로 인식, 구두점 2개 이상 경고 ⑨ T·N 규칙에서 '작업에 걸린 시간'을 (B) 주기형으로 한정하고 예시(반나절·하루에) 교체, user 블록에 '걸린 시간·기간은 담당자 수치·메모에 있을 때만' 한 줄. 회귀: `title_test.js` ⑧I(일괄 초안)·⑫(9건), `servertest.mjs` 25절 9/6 항목 10건.
- 후속 킵: 안내서 `05_홍보_검색노출/블로그_게시_안내_권민지.html` 검수 절 한 줄(설계 §6-4) — 앱 밖 문서라 이 커밋 범위 밖.

## 3. 서류함 + AI 판독 (하드리딩)

계약 카드의 서류함에 파일 첨부(버튼/드래그) → 파일명으로 자동 분류·백그라운드 판독(30초~2분):

| 파일명에 | 판독 종류 | 추출 → 저장 |
|---|---|---|
| 석면/조사 | 석면조사서 | 자재표(동·자재·위치·면적·함유율)·의뢰인·연면적 → §94 노동부 신고대상 자동 판정 → **검수 모달에서 사람이 확정** |
| 계약 | 계약서 | 공사명·금액·계약/착공/준공일·발주자·주소 → 계약 단계 자동 완료 제안 + 착공계 작성 제안 |
| 대장 | 건축물대장 | 용도·구조·연면적·층수·승인일 |
| 등록증/신분증 | 발주처 증빙 | 상호(성명)·대표·사업자번호(개인=생년월일)·주소 (주민번호 뒷자리 추출 금지) |

**입력 우선순위**: ①앱이 알아서(자동 조회·기존값) → ②첨부 판독 → ③수기(최후). 원문에 없는 값만 생성 직전 "확인요망" 목록으로 확인.

**건축물대장 자동 조회**: 계약 카드 [대장] 버튼(지번 주소) 또는 계약서 판독 시 자동 — 건축HUB API, 동별 용도·구조·연면적. 카드에 🏢 칩.

## 4. 서류 자동 생성 (xlsx 수술 채움)

관리자 [+ 양식 등록]으로 원본 xlsx를 서버에 영구 보관(오염 방지) → 생성 시 원본 복사에 값만 교체(서식·도형·인쇄 100% 보존).

| 양식 | 자동 채움 |
|---|---|
| 착공계 | 기본정보(공사명·주소·한글금액·날짜) → 공문·공정표(공종별 세트)·직접시공계획서·현장대리인계(일용직 명부에서 대리인 선택) 12개 서식 전파 |
| 준공계 | 위 + 준공검사원·청구서(계좌·금액) |
| 석면해체계획서 | 검수 자재표 5행·함유율·준공일자(대장 승인일)·명단 4곳(일용직 체크 선택+인원수)·근로자대표 |
| 해체신고서(완료신고 포함) | 관리자·위치·해체기간 + **동별 개요=대장 데이터** → 위임장·해체공사계획서·완료신고 수식 전파 |
| 폐기물배출신고서(수탁확인 포함) | 발주자(신분증/사업자등록증 판독)·용역명·현장·폐석면/부자재 톤수·제출년월·수신처(주소에서 시장/군수 자동) → 수탁확인서 1·2p·유해성자료 수식 전파. 시행=종운환경 고정 |
| 적격심사 패킷(hwpx) | 별지1 신청서+접수증·별지2 심사표·별지4 각서·별지5 이행확약서 일체. **낙찰 감지→D-7 지시(별표3 체크리스트 포함)→배너 [계약 시작]→전 값 자동 프리필(공고번호·입찰일·낙찰금액·A값·예정가격(수집봇 plnprc)·구간·등급)→자기평점 자동(김천서부초 실물 심사표로 산식 검증 완료)→패킷 생성→증빙 보관함 일괄 다운로드**까지 원스톱. 기준 세트 2종(교육청 2023/지자체 별표7), 시행 회사=계약 유형 규칙 |

| 견적서(hwpx) | 회사별 사내 공식 양식(JW-03-03-001-01 환경/JW-03-03-001-02 건설 — 문서체계 v2, 구 JW-06-001/002). 수의계약 요소 체크 시 "견적" 단계 자동 조립 → [서류]에서 수신처·용역명·품목 1행·부가세 구분 입력 → 금액(수량×단가)·한글금액·견적번호·일자 자동. 합계는 한컴 계산식 보존(2행째부터 수기 시 자동합산). **파이프라인과 별개로 발행하려면 [견적서] 탭 사용**(아래 2.5) |
| 선금지급신청 세트(hwpx) | 공문+별첨2(신청서)+별첨3(각서)+별첨4(사용계획)+별첨5(청구서)+지급조건 일체(김천서부초 제출 실물 역산, 교육청 계열). 차액 C=A−B·적용요율 D=E/C(50% 초과 경고)·잔액·사용계획 소계/부가세(총계÷1.1 절사)·한글금액 전부 자동 계산. 착공 후 "선급금" 단계·서류 종류에서 생성 |
| 계약서류 2종(hwpx) | 계약이행 통합서약서(포항시 양식 4쪽) + 사용인감계. 계약명·보증기간(계약일~준공일)·한글/숫자 금액·보증금(률×금액)·회사별 상호/연락처/사업자번호·수신처(맑은물사업본부 자동 제안). 입찰 건은 6·7번 체크 수정 안내 |

**hwpx 수술 채움**: hwpx=zip+xml — 등록 템플릿의 `{{토큰}}`을 교체(셀 좌표 불필요). 토큰이 든 문단은 줄배치 캐시(lineseg)를 1줄로 축소해 한컴 열기 거부 방지(긴 고정 문단은 보존 — 전역 축소 시 렌더 뭉개짐, 실측). 템플릿 제작은 한컴 COM으로 1회(원본 hwp→AllReplace 치환→hwpx 저장, SetMessageBoxMode(0x10001)로 대화상자 차단). 적격심사→qual_packet, 계약서류→con_docs 단계 자동 라우팅.

**계약서 작성 툴**(비입찰·개인): 계약 단계 [서류] → 간편 입력(발주처·주소·금액만, 증빙 첨부 시 갑란 자동) → 생성하면 계약에 저장 → 대장 자동 조회 → 계약 단계 완료 + 후속 서류 일괄 생성(공문서=착공계+, **개인 사문서=착공계·준공계 제외**). 제목: ☑석면해체/☑철거공사 체크 2줄+계약서.

**석면 노동부신고서류**: 조사서 검수 [확정] 순간 "석면해체계획서 생성할까요?" 자동 제안.

**내역서 낙찰률 적용(v2)**: [생성]→내역서 낙찰률 적용 — '단가' 열 숫자만 ×낙찰률 **내림**(실물 관행 실측), 수식 보존. **정밀 안착**: 적용본의 재계산 총원가·이윤을 모달에 입력하면 "이윤 셀을 N으로 수정" 정확값 제시(총원가=낙찰가÷1.1 역산, 부가세 절사/반올림 규칙 자동 판별). 철강공단 실물 쌍으로 단가 0원 일치·도급액 정확 안착까지 COM 검증.

## 4.5 알림 (웹푸시 1차)

로그인 모달 [🔔 이 기기 알림 켜기] — 기기별 구독(회원당 5기기, Blobs `push:subs`). VAPID 키는 서버 최초 사용 시 자동 생성(`push:keys`, 환경변수 불필요). 발송: ①지시 등록→담당자 ②완료 체크→승인권자(지시자, 없으면 관리자들 — 본인 제외) ③승인/반려→담당자 ④계약 단계 완료요청→승인권자 ⑤개찰결과(수집봇)→관리자 전원 ⑥만기 지시 자동생성→관리자들. 알림 클릭=앱 포커스. 카톡 연동은 후속(웹푸시로 충분한지 사용 후 판단).

## 4.6 결재함 (대표 결재 — 1차 C안 + 2차)

전용 화면 없이 상단 뱃지 → 오버레이 한 장씩 처리(승인/반려·사유 필수/보류). 저장은 approvals 컬렉션 전용 액션 3종(목록=비관리자 본인분만, 결정=관리자+낙관락). 대표 홈=결재함(대기·보류 카드+[결재 시작]). 종류: 지시·운반일지·사규·지입료·가족친화. 결과(승인/반려+사유)는 기안자에게 웹푸시.

**2차(v306, 2026-09-02)**:
- **내 상신 목록**: 직원·관리자 홈에 "🧾 내 상신" 섹션(제목·종류·상태·반려 사유·경과일) — 대기·보류 먼저, 최근 10건, 0건이면 섹션 생략. 비관리자는 60초 캐시로 별도 조회. 대표 홈은 제외(대표=결재자)
- **우선기기 1발**(배치도 결정 ③): 로그인 모달 [⭐ 우선기기] 버튼 — 알림 켜진 기기에서 지정·해제 토글(회원당 1대, `push:subs` primary 플래그). 결재 요청 푸시는 우선기기에만 발송, 미지정자는 전 기기 폴백. **운반일지 결재 요청만 전 기기**(결정 ① 예외 — 오피스PC 팝업+폰 병행). 다른 알림(지시·개찰 등)은 현행 유지
- **운반일지 자동 기안**: 올바로 수집 성공 시 워커가 KST 오늘-2 일자(완성 규칙)를 자동 상신 — 기안자 "자동상신(올바로)". 운반 0회=스킵, 같은 날짜 상신이 상태 불문 존재하면 스킵(수동 상신 존중·반려 재기안 루프 방지), 멱등 cid(`auto-ab-날짜`)로 재실행 중복 차단, approvals 읽기 실패 시 기안 스킵(fail-closed)

**운반일지 = 대표 전용 결재(v308, 2026-09-03, PM 결정 "운반일지는 나는 확인만")**: 운반일지 종류(자동 기안·수동·정정)는 `to:'boss'`로 스탬프되어 대표(role 대표 또는 나종운, admin)만 결재 대기·뱃지·한장씩 큐·PC 팝업에 잡힌다. 결재 요청 푸시도 대표에게만(전 기기 유지, 대표 계정이 없으면 푸시·결재 모두 관리자 전원 폴백). 다른 관리자는 🔔 알림함 이력("결재 요청: 운반일지 …")과 결재 결과 통지(우선기기 1발)로 확인만. 서버 하드 게이트: 승인·반려는 대표만(`BOSS_ONLY` 403 — 대표 계정이 하나도 없으면 관리자 전원에게 열림, 클라도 `boss_present`로 같은 폴백), 보류는 서버 규칙상 관리자 누구나(현행 화면은 비대표 관리자에게 대표 전용 건을 보여주지 않아 사실상 대표만). 9/2 이전 생성된 `to` 없는 운반일지 건도 종류로 대표 전용 취급.

**3차 — 업무별 결재 등급(v311, 2026-09-03, 명세 시안_결재등급_명세_20260903.md §2 전량 확정)**: 업무 종류마다 등급이 붙는다 — ①PM 전결(담당→PM 승인=종결, 대표는 매월 1일 총정리 카드 [확인]만) / ②결재라인(담당→PM 승인(1/2)→대표 승인(2/2)) / ③대표 전결(담당→대표, PM은 알림함 확인만 — v308 운반일지의 일반화). 배정(§2): ③=운반일지·지입료, ②=사직·휴직, ①=지시·사규·매뉴얼·가족친화·휴가·계약 단계·청구 검수·문서함 등재·견적·인허가 등재·조기퇴근(후보 종류는 각 모듈 결재 훅이 붙을 때부터 — kind 문자열이 표와 같아야 함. **문서함 등재는 v314에서 훅 연결** — §4.9).
- **등급표**: `settings:approvals` blob(`appr_grades_get/set`, admin 전용·변경 감사로그 `등급변경`). 관리 화면 = 권한관리 탭 하단 "결재 등급표"(종류×등급 칩 — ①②③·현행). 기안 시점 `item.grade` 스냅샷이라 표 변경은 새 상신부터(진행 중 건 영향 없음).
- **생성 스탬프**(approval_create): kind→grade, `to`=③이면 'boss'·①②는 'pm'. "대표 상신" 토글=① 건별 ② 격상(`escalated`). PM(비대표 관리자) 자기 기안 ②는 PM 단계 자동통과(chain '자동통과', 즉시 대표 큐). `전결총정리` kind는 사용자 기안 불가(400 SYSTEM_KIND). grade 없는 구건·등급표 밖 kind=현행 그대로.
- **게이트**(approval_decide, §4.1): grade 있고 `to:'pm'`=비대표 관리자만(대표는 403 PM_ONLY — 비대표 관리자 0명이면 대표에게 폴백), `to:'boss'`=대표만(기존 BOSS_ONLY·폴백 유지). ② PM 승인=`to:'boss'` 전환(status '대기' 유지·decided_* 미기록·대표 우선기기 1발·기안자는 알림함 줄만), ② PM 단계 보류 불가(400 HOLD_NOT_ALLOWED). 반려는 어느 단계든 종결·기안자 회귀. 등급 건은 `chain`에 단계별 기록.
- **화면**: 카드·대표 홈·내 상신에 등급 뱃지(①/②/③/미정)와 단계 줄("PM 나경일 승인 09-02 → 대표 (2/2)"), 내 상신 상태 문구("PM 대기 (1/2)"→"PM 승인 완료 · 대표 대기"→결과, ① 승인="승인 · 월간 총정리 포함"). PM 뱃지·큐에서 ③건 제외(알림함 줄만 — v308 방식), 대표 뱃지·큐에서 ①·② 1단계 제외. ② 1단계 버튼=[승인 → 대표로/반려], 운반일지=[확인(결재)] 라벨.
- **전결 총정리**(`gw-appr-cron`, 매일 UTC 23:00 기동·KST 1일 08:00만 실행): 전월 ① 승인 건 + 휴가 모듈 승인(읽기 합산 — 이중 결재 없음, 사직·휴직 제외)을 카드 1장(`summary-YYYY-MM`, kind 전결총정리·③·to boss)으로 대표에게 상신. 0건 미생성·재실행 멱등·fail-closed·감사로그·대표 우선기기 1발(PC 팝업 없음). 버튼 [확인] 하나=decide '확인'(status '승인', 감사로그 "열람 확인"). 반려·보류 불가(400). 미확인 재푸시 없음 — 다음 달 카드 상단에 "전월 총정리 미확인" 1줄. 분기 총정리는 미구현(기본 꺼짐, PM 결정 대기).

**직접 기안 + PM 전결 종결(v315, 2026-09-04 — PM "사규·가족친화 결재는 어딨노?")**: 등급표엔 ① 11종이 있는데 기안 UI는 지시 탭·운반일지 버튼·문서함 등재 게이트뿐이라 PM이 사규·매뉴얼·가족친화 등을 직접 기안해 전결 종결할 화면이 없었다. PM 전결의 뜻 = 기안자가 비대표 관리자이고 ①이면 **기안=결재**(절차 두 번 금지).
- **[✍ 기안] 버튼**(상단 결재 뱃지 옆 — 로그인한 관리자·직원 모두, **대표 계정만 숨김**: 대표는 기안 대상이 아니라 결재자) → 모달: 종류(등급표 kind 중 전용 경로가 있는 **지시(지시 탭 전용)·운반일지·문서함 등재·휴가·전결총정리 제외** — `APPR_DRAFT_EXCLUDE`=서버 `APPR_SELF_DECIDE_EXCLUDE`, uismoke 대조. 카드의 "원 지시를 찾을 수 없음" 고아 힌트는 ref가 지시 id 형식일 때만)·제목(필수)·내용(≤500)·참조 문서(문서함 항목 선택 → `ref 'doc:<id>'`, 미선택=공란)·종류 옆 등급·흐름 미리보기("① PM 전결 — 기안=결재(전결 종결), 월 총정리로 대표 확인" / "② 담당→PM→대표" / "③ 대표 전결 — PM은 알림함 확인만"). 등급은 `approvals_list` 응답에 동봉되는 등급표 스냅샷(`grades` — 표시용, 관리자·직원 공통)으로 그린다; 진짜 등급은 서버가 기안 시 다시 읽어 스탬프.
- **직원·②·③ 기안 = [상신]**: 기존 라우팅 그대로(① PM 큐 / ② PM 1단계 / ③ 대표 큐) — 상신 대기열(`apprPendingCreates`, 오프라인 영속·직렬·cid 멱등) 경유.
- **PM(비대표 관리자) + ① = [전결 종결] 주버튼 + [상신만] 보조**: `approval_create` `self_decide:true` → 서버가 **카드 생성과 '승인'을 한 번의 쓰기로**(status '승인'·decided_by=기안자·decided_at=created·`self_decided:true`·chain `전결`·감사로그 **`전결`** 1건 — create+decide 두 왕복 없음, 결재 큐·뱃지에 잠깐도 안 뜸, 푸시 없음). 거부는 전부 400(클라가 [상신만]으로 되돌리게): 직원·대표 `SELF_DECIDE_PM_ONLY` / ②·③·미정·격상(대표 상신 토글) `SELF_DECIDE_GRADE1_ONLY` / 제외 종류·`ab:` ref `SELF_DECIDE_KIND`. cid 멱등(재시도=같은 id·`decided '승인'`). `apprCreateItem` 본체가 최후 방어선(서버 내부 훅이 실수로 넘겨도 열리지 않음). [상신만]은 PM 큐에 두는 종전 경로(그 뒤 본인이 결재함에서 승인).
- **기록**: '내 상신'에 "전결 종결 · 9/4 · 월간 총정리 포함" / 카드·내 상신에 참조 문서 제목("📎 참조 문서: … — 문서함 **등재**와는 별개의 결재 기록입니다" — 사규 문서의 '등재'(문서함 게이트)와 사규 '결재'(이 기안)는 별개 행위) / 총정리 크론은 grade1+승인+decided_at(전월)로 집계하므로 전결 건이 그대로 포함(servertest 검증).

## 4.7 진척도 탭 (관리자 전용, v307, 2026-09-03)

사이드바 관리 그룹 마지막 "진척도" — 관리자(admin)에게만 보이고, 본문은 `gw-progress` 함수가 세션+관리자(또는 개발자)를 검증한 뒤 JSON `{html}`로 준다. 화면은 sandbox iframe(srcdoc, allow-scripts·allow-popups·allow-popups-to-escape-sandbox — same-origin 없음, 문서 링크는 rel=noopener 새 창)이라 앱 토큰·저장소에 닿지 못한다. 헤더 [↻ 새로고침]으로 재요청, 탭 재클릭은 재로드하지 않음.
- 원천: `netlify/functions/_lib/progress.json`(정적 require로 esbuild 인라인 번들) — `00_진척도/진척도_빌드.js`가 진척도_모바일.html 전문을 doctype 뼈대와 함께 써 넣는다. 갱신 = 빌더 실행 후 커밋·푸시(w/ 2종과 같은 커밋).
- 정적 노출 차단: `publish="."`라 레포 파일이 전부 열리고 Netlify 정적 서빙은 대소문자·중복 슬래시를 무시하므로(9/3 실측 `/NETLIFY/functions/gw-auth.js` 200) toml 재작성만으론 부족 — **Edge Function `netlify/edge-functions/guard.ts`**가 모든 요청의 경로를 정규화(디코드·소문자·슬래시 정리)한 뒤 `/netlify/*`·`/tools/*`·`*.md`·`package*.json`을 404로 끊는다(가드 자체 오류 시 통과). toml `[[redirects]] /netlify/* → gw-progress(force)`는 2중 방어. `/w/*`(무인증 벽지·위젯)에는 `X-Robots-Tag: noindex`·`Referrer-Policy: no-referrer`. 벽지 wd 파일(직원명 포함 전문)은 파일명이 열쇠인 채 유지 — 축약판 전환·폐지는 PM 결정 대기.
- 감사로그 col `progress` op `열람`(누가 언제 봤는지).

## 4.8 내 할 일 기한·알림 (v309, 2026-09-03)

내 업무 탭 "내가 할 일"(회원별 비공개 mytasks)에 기한(due, 선택) 추가 — 추가 행의 날짜칸, 미완료 행의 인라인 날짜칸(change 시 즉시 저장), 지시와 같은 D-day 배지(경과 빨강·3일 내 주황), 기한순 정렬. 직원 홈 "🗒 내 할 일"도 같은 행 렌더러라 배지·인라인 기한이 함께 보인다.
- 알림: `gw-todo-cron`(KST 08:00 매일)이 회원별 문서를 훑어 **오늘 기한·미완료** 항목이 있으면 그 회원에게만 웹푸시 1발("오늘 기한 할 일 N건" — 제목은 싣지 않는다: 알림함 이력은 관리자 전원이 보므로, 전 기기). 항목·날짜당 1회(`todo:sent:<mid>` — 기한을 미루면 새 기한일에 다시), 완료·삭제된 항목은 발송 안 함, 경과 재알림 없음(배지로만). 사용자 문서는 되쓰지 않는다.
- 전제: 직원 기기에서 [🔔 이 기기 알림 켜기]가 켜져 있어야 도착한다(안 켰으면 🔔 알림함 이력만).

## 4.9 문서함 공개범위·등재 결재 (v314, 2026-09-04)

PM 지시 "문서함 일단 다 올리고 등재결재랑 공개범위 만들어라 내가 선택하게" + 추가 "비공개로 다 올리라고". 설정은 권한관리 탭 하단 **"문서함 설정"** 카드(등급표와 같은 칩 UI·낙관락 `DOC_SETTINGS_STALE`·감사로그 `문서함설정`, `settings:documents` blob, `doc_settings_get/set` admin 전용).
- **공개범위(scope)**: 문서마다 `scope` = 전원(`all`) / 관리부+관리자(`mgmt` — 01 법인 docCanSee01과 같은 축) / 관리자만(`admin`) / 직원 지정(`{ids:[회원id]}`, +관리자). 미지정=**분류 기본값**(설정 카드 — 02·03·05·06·미분류. **초기값 전부 '관리자만' = 전부 비공개**, 공개할 분류만 PM이 연다). 01 법인은 관리부+관리자 하드차단 유지·설정 불가(문서 scope로 더 좁힐 수만 있음). 수정 모달 "공개범위" 셀렉트(+직원 체크 상자 — 지시 모듈 개별 직원 상자와 같은 결), 행 배지 "공개 …"(미지정은 "분류 기본(관리자만)").
- **서버 하드차단**(`docVisible` — get 필터·save 재구성 공용): get에서 비관리자는 (문서 scope 또는 분류 기본값)에 안 맞는 문서·타인의 대기/반려 문서가 제거된다. save에서 비관리자가 못 보는 문서(01 포함)는 서버 원본으로 재구성(id 기준 — 지우거나 cat·scope를 바꿔 내보내는 탈취·열람 필터로 빠진 사본이 남의 문서를 지우는 사고 차단). 직전 문서 읽기 실패 시 비관리자 저장 거부(`PREV_READ_FAILED`, licenses duties와 같은 원칙). 관리자 무제한. 등재한 본인(`by.id`)은 자기 문서를 항상 본다(승인 뒤 사라지는 혼란 방지 — 단 **01 하드차단이 본인 예외보다 앞**: 관리자가 직원 문서를 01로 옮기면 그 직원도 못 본다). del:1 항목은 규칙만 맞으면 그대로 내려간다(소프트 삭제·복구는 클라 몫 — 영구 삭제 없음). **비관리자의 기존 문서 편집은 내용만**(제목·버전·개정일·링크·메모) — 분류(cat은 원본 파생값을 명시 고정)·문서번호·구 분류 텍스트·공개범위는 서버 원본 고정(관리부원이 mgmt→all·ids 확장·05→02 갈아타기로 무결재 노출을 만들지 못하게, 적대 검증 9/4 med1), 클라 모달도 잠금("PM만 변경"). 기존 문서를 cat '01'로 보내도 원본 유지(소실 0), 신규 01은 폐기+감사로그 `제거`(med3).
- **등재 결재(register_gate)**: 문서 `status` = `등재`(구건·status 없음 포함) / `대기` / `반려`. **관리자가 추가·수정하면 항상 즉시 '등재'**(등급표 ① PM 전결 — 올리는 행위가 결재. 반려·대기 건도 관리자가 손대면 등재, 남은 카드는 기록용). **비관리자(doc 수행 권한자)가 추가**하면 게이트 `staff`(기본)=`대기` + 서버가 결재함에 **'문서함 등재' 카드 자동 상신**(기안자=등재 직원, ref `doc:<id>`, cid `docreg-<id>` 멱등, 본문=번호·분류·공개범위·링크·메모 — `apprCreateItem` 공용 본체(사용자 기안과 같은 규칙: 등급표 ①·PM 큐·우선기기 1발), 상신 실패는 감사로그 `등재상신실패` + 응답 `register_warn`으로 직원에게 alert + 그 직원의 다음 문서 저장에서 같은 cid로 재시도 + **관리자 결재함 폴(approvals_list)의 반대 방향 복구** — '대기'인데 현재 cid의 카드가 없는 문서를 등재자 명의로 대신 기안(`docRegisterReconcileCreate`, 감사로그 `등재상신복구`, 방금 만든 카드는 그 응답에 즉시 포함 — med2) / `none`=즉시 등재(registered_by=본인). 서버가 기존 문서의 status·by·등재 기록을 원본 고정(비관리자가 '등재'로 올리는 시도 원복). 재상신은 **본인 반려 건 + 클라가 `reg_n`을 정확히 +1로 보낸 명시 신호**만 인정 — 낡은 '대기' 사본 편집(409 병합 로컬 승리)이 재상신으로 오판돼 반려 사유가 사라지지 않게(low4).
- **결재 훅**(approval_decide → `docRegisterApply`): 승인 → 문서 `등재`(registered_by/at) / 반려 → `반려`(reject_reason). **결재 상태 저장이 먼저**, 문서 반영은 그 뒤(실패해도 결재 결과 확정 — 감사로그 `등재반영실패`, 관리자 결재함 폴(approvals_list)이 최근 7일 결정분을 멱등 재시도). 반영 조건 = 카드 cid가 문서의 현재 등재 cid와 같고 문서가 '대기'일 때만(재상신된 문서에 구 카드가 손대지 않음·관리자가 직접 등재한 문서는 카드가 기록용·v313 구 카드 무변경). 감사로그 `등재`/`등재반려`(col documents). 반려 건은 올린 본인이 행 [재상신] → `대기`·`reg_n`+1 → 새 cid `docreg-<id>-<n>` 카드. 승인 시 반려 잔존(reject_reason·rejected_at) 정리. 쓰기 뒤 자가복구(동시 저장 되돌림 재적용)도 문서가 여전히 '대기'·같은 cid일 때만 — 그 사이 관리자가 직접 등재한 문서를 뒤늦은 반려가 뒤집지 않는다(low7). 문서 블롭 결재 경로 변경 시에도 버전 링 스냅샷(변형 전 상태).
- **화면**: 행 배지 "등재 대기"/"반려 — 사유"·등재자, 결재 카드에 문서 현재 상태 힌트(등재됨/삭제됨), 결재 후 문서함 재조회. 저장 직후 재조회로 서버 스탬프 반영, 409 충돌은 id·updated_ts 병합 후 1회 재시도(`mergeDocs` — 서버가 결재 경로로 문서를 바꾸게 되면서 필요해진 경로. 삭제도 `updated_ts`를 찍어 서버 갱신본이 삭제를 되살리지 않게 — low5).
- v313의 문서 행 [결재 상신](종류 4종 prompt, `docApprSubmit`)은 PM 전결 원칙과 어긋난 임시물이라 **제거** — 등재 결재 훅으로 대체.
- 배포 전 임시 조치(PM 9/4): col:documents 18건에 `del:1, hidden_tmp:1`을 박아 숨겨 둠 — 코드는 소프트 삭제로만 취급(서버 재구성·클라 모두 영구 삭제 없음), 배포 후 PM이 블롭에서 del 해제해 복원. 복원 즉시 비관리자에겐 전부 비노출(분류 기본 관리자만)이 정상.

**문서함 파일 첨부 + 일괄 등재(v315, 2026-09-04 — PM "실물 없는 포인터 목록")**: 문서함 항목이 url·메모뿐이라 실파일이 없었다. 계약 첨부 저장소(`gw_files`, base64 8MB)를 재사용해 문서마다 실파일을 붙인다.
- **저장**: 바이트=`gw_files` `docatt:<docId>:<n>`(문서당 여러 파일, 상한 20), 메타=문서 항목 `files:[{n,name,size,mime,ts,by}]` + **`att_seq` 카운터**(n은 카운터에서만 발급 — 삭제 후 재사용 없음, 카운터 없는 구건은 files 최대 n에서 이어감. 적대 검증 low4·low7). **files·att_seq는 첨부 액션으로만 바뀐다** — `save` 재구성이 관리자·비관리자 모두 서버 원본을 이월(`docFilesFix`: 비관리자 위조·삭제 차단 + 낡은 사본 저장이 첨부 목록을 지우는 사고 차단, 신규 문서는 files 0으로 시작). **관리자 저장도 직전 문서 읽기 실패면 500 `PREV_READ_FAILED`**(med2 — fail-open이면 files 전량 삭제·스냅샷 없이 덮는다).
- **mime·내용 검증(high1 — Blob URL 저장형 XSS 차단)**: 클라가 보낸 mime은 저장·응답 어디에도 쓰지 않는다. 서버 `DOC_ATT_MIME` 확장자→mime 고정표(pdf/docx/xlsx/pptx/hwp/hwpx/jpg/jpeg/png — 키는 `DOC_ATT_EXT` 화이트리스트와 동률, 앱 표와 값까지 동률 uismoke)로 type을 강제 저장하고 get도 저장값이 아니라 표의 값만 돌려준다. 확장자 검사 뒤 **매직바이트 최소 검증**(pdf `%PDF` / png 시그니처 / jpg `FF D8 FF` / zip 계열 docx·xlsx·pptx·hwpx `PK 03 04|05 06|07 08` / hwp OLE `D0 CF 11 E0` 또는 3.x 텍스트 머리) — 불일치는 400 `BAD_MAGIC`. 클라도 열 때 응답 type을 무시하고 자기 고정표(`DOC_ATT_MIME`)만 쓴다(표 밖은 octet-stream 다운로드). 화이트리스트 조회는 전부 hasOwnProperty(low5 — `constructor` 등 프로토타입 키 차단).
- **액션**: `doc_att_put`(문서 id·파일명·base64 — **base64 6MB = 원본 ≈4.5MB**(`DOC_ATT_MAX`, Netlify 동기 함수 본문 한도 ~6MB 안쪽 — med3. 계약 첨부 `ATT_MAX` 8MB는 별개·불변) · 관리자 또는 등재 본인(`by.id`=본인 + doc 수행) — 본인은 '대기' 문서에도 첨부 가능 / 삭제·없는 문서 404 / 쓰기 후 재확인에서 같은 n에 남의 첨부가 착지했으면 409 `ATT_CONFLICT`(클라 재시도=새 n)) / `doc_att_get`(열람 판정 = `docVisible` — 공개범위 하드차단과 같은 축, 관리자 무제한, 못 보는 문서는 404 NO_DOC로 존재 비노출, doc 권한 hide는 403) / `doc_att_del`(관리자 — 메타 제거가 먼저, 바이트 삭제 실패 시 고아 블롭만 남고 열기는 404, att_seq 유지). `att_parse`도 `att_` 프리픽스 강제(low6 — 계약 권한으로 `docatt:` 판독 차단). 감사로그 `첨부`/`첨부삭제`(col documents). 메타 변경은 사람 저장과 같은 버전 링 스냅샷 + 쓰기 후 1회 자가복구, 응답의 `updated_at`으로 클라 낙관락 base 갱신.
- **화면**: 문서 수정 모달 하단 "첨부 파일" — 목록(열기·관리자 삭제)+[📎 파일 첨부](파일 선택 → 선택 즉시 확장자·**4.5MB** 검사(초과 alert) → base64 → 직렬 업로드) — 올리기 버튼은 관리자·등재 본인만. **신규 문서는 저장 후 [수정]에서 첨부**(id 확정 뒤). 문서 행에 `📎 n` 배지 + 파일마다 [열기](`doc_att_get` → Blob URL: pdf·jpg·png는 새 창 뷰어, hwp·docx 등은 다운로드). 기안 모달의 참조 문서 목록에도 `📎n` 표시.
- **일괄 등재 `doc_bulk_put`**(관리자 전용, 스크립트용 — 정본 61건+증빙): `{action:'doc_bulk_put', cid, items:[{title,cat?,no?,version?,revised?,scope?,url?,note?,files:[{name,data(base64)}]}]}`(mime은 보내도 무시 — 확장자 고정표) — 항목당 첨부 **1~3**, 파일당 base64 6MB, 요청당 첨부 base64 합계 **5.5MB**(JSON 오버헤드 포함 본문 6MB 안쪽), 항목 100, **전량 검증 뒤에만 쓴다**(하나라도 틀리면 전체 400/413 + `index`(·`file`) — 반쯤 등재된 상태 없음; 매직 불일치 `BAD_MAGIC` 포함). 관리자 등재=전결 즉시 '등재'(registered_by·by=관리자), scope 미지정=분류 기본(비공개), cat은 2층 분류 73키("AA-BB"·"99") 화이트리스트 밖(구 2자리 포함)이면 99. **cid 멱등**(`docbulk:<cid>` 블롭 — 같은 cid 재요청은 첫 결과 그대로 `dedup:true`). 문서 항목에 `bulk_cid`·`att_seq` 스탬프, 감사로그 `일괄등재`(30건 초과분 생략 표기), 버전 링 스냅샷·자가복구.

**문서함 1층 분류 12종 확장(v315, 문서번호체계_개편안_20260904.md §2·§5 — v317 2층 체계로 대체, 이력만)**: 01 법인·등기 … 12 홍보·소개 + 99. `DOC_CATS` 13키 정적 표·`docCatOf` 정규식 `/JW-?(0[1-9]|1[0-2])(?!\d)/i`·구 텍스트 폴백. 아래 v317이 전부 대체.

**문서함 2층 분류 체계 v2(v317, 2026-09-04 — 문서체계_설계안_v2_20260904.md §2·§6, PM 9/4 14:20 확정·§9 4번만 반려)**: 1층 대분류 = 업무 영역 12(01 법인·등기 / 02 경영·총무 / 03 영업·홍보 / 04 계약·공사 / 05 차량·장비 / 06 안전보건 / 07 인사·노무 / 08 인허가 / 09 인증·경영시스템 / 10 재무·세무 / 11 정보·시스템 / 12 기타) + 99 미분류(앱 전용), 2층 중분류 = 문서 성격 6(01 규정·기준 / 02 매뉴얼·가이드 / 03 양식·서식 / 04 관리표·대장 / 05 계획·조직 / 06 자료). 문서 `cat` = `"AA-BB"`(73키 화이트리스트 = 72 조합 + `"99"`), 문서번호 4층 `JW-AA-BB-NNN[-SS|-YYYY]`(예 JW-06-01-004-01 위험성평가표 양식 = 위험성평가 실시규정 JW-06-01-004의 별지, JW-06-05-001-2026 연도판 — 문자열 정렬이 계층 순서와 같아 v316 번호순 정렬 그대로). 번호는 문서대장이 발급, 앱은 만들지 않는다.
- **앱**: `DOC_MAJOR`·`DOC_MAJOR_ORDER`·`DOC_MINOR`·`DOC_MINOR_ORDER` → `DOC_CATS`(라벨 "06-01 안전보건 · 규정·기준")·`DOC_CAT_ORDER`(73) 생성. `docCatOf`: cat 화이트리스트 우선 → 정규식 `/JW-?(\d{2})-(\d{2})-\d{3}/i`의 앞 두 마디(키에 있을 때만) → 99. **구형식 JW-05-001·구 2자리 cat('03' 등)·번호 없음 → 99**(오배정 대신 미분류, 구 텍스트 폴백 삭제). `docMajorOf` = 대분류 2자리 — 01 하드차단 판정(수정 제한·편집 버튼·신규 지정 차단)과 공개범위 설정 조회 공용(**설정 키 = 대분류**, 중분류 단위 설정 없음); 구 cat `'01'`은 번호 파생이 실패해도 01(마이그레이션 전 법인 문서가 99로 풀려 문서 scope로 노출되지 않게 — 서버 동일). 모달 분류 = 2단 select `#docCatMajor` → `#docCatMinor`(`docCatSelectsInit/Set/Get/Lock`, 신규 기본 99·중분류 필수·99/01 지정은 관리자만·비관리자 기존 문서는 둘 다 잠김). 문서함 서랍 2층(대분류 서랍: 합계 건수·01 🔒 → 중분류 서랍: 건수 → 행 번호순; `docOpenCats` 키 "AA"·"AA-BB"; 빈 중분류는 숨김, 빈 대분류는 관리자에게만 0건 표시, 99 미분류 서랍은 서버가 내려준 문서가 있을 때만(열람은 문서 scope·설정 카드의 99 행이 정함 — v318)·대분류명 검색 가능). 설정 카드 12행 라벨 `DOC_MAJOR`. 체크리스트 form 21행·견적서 주석 2행·placeholder는 §5.1 대조표로 치환(JW-06-013→JW-05-01-001-01 등).
- **서버**: `DOC_MAJOR_LABEL`·`DOC_MINOR_LABEL`·`DOC_MAJOR_SET`·`DOC_MINOR_SET` → `DOC_CAT_SET`·`DOC_CAT_LABEL`(결재 카드 "분류 06-01 안전보건 · 규정·기준") 생성, `docCatOf`(앱과 정규식 동률)·`docMajorOf`. `docScopeOf`(분류 기본값 = 대분류 키)·`docVisible`·save 재구성(못 보는 문서·01 keep, 신규 01 폐기)의 01 판정 = `docMajorOf`. `DOC_SCOPE_CATS`·`DOC_SETTINGS_DEFAULTS`·`doc_settings_set BAD_CAT`는 대분류 키 그대로(중분류 키 '06-01' 설정 → BAD_CAT). `doc_bulk_put` cat 화이트리스트 73키(구 2자리·표 밖 → 99 강등).
- **데이터 마이그레이션(설계 §6.4·§7 — 이 커밋 밖, 배포 직후 같은 작업 시간)**: 활성 61건 `cat`("AA-BB")·`no`(4층 전체 번호)·`title`([법인] 단서·변형/연도 괄호) 갱신 + 견적서 hwp 2건을 docx 항목 2번째 첨부로 병합 → 59건. 그 사이 활성 문서는 관리자에게 전부 99 미분류로 보이고 직원 노출 0(전 대분류 기본 '관리자만'). 소프트 삭제 30건 불변. `settings:documents` 조치 없음.
- sw v317. servertest(절 19·20·22 fixture 2층 cat + 절 23 교체: 대분류 설정 12키·BAD_CAT 중분류 키·4층 파생 11건·구 cat 01 하드차단)·uismoke(대분류·중분류 키/순서/라벨 동률·조합 73·설정 대상·정규식 동률+의미·2단 select·구 잔재 0).

## 4.10 관리자 등급·문서함 휴지통·기안 참조 검색·화관법 문구 회수 (v321, 2026-09-06, PM 9/6 ㄱ)

**관리자 등급(tier) 세분화**: 회원(관리자만)에 `tier` = `boss`(대표) / `pm`(PM) / `admin`(관리자). 관리자가 아닌 회원·삭제·퇴사(leave_date 경과) 회원은 등급 없음. **게이트는 명시 tier만 신뢰**(9/6 검증 S1 반영 — `_lib/tier.js tierOf(m, bootstrap)` = 앱 `tierOfMember`, uismoke 동률 대조): 명시 tier 없는 관리자는 `admin`(최하 등급 — role·이름·dev 불신). **부트스트랩 예외** — 재직 관리자 중 명시 tier가 한 명도 없을 때만(배포 직후·등급 지정 전) 종전 파생(role '대표' 또는 이름 나종운 → boss / dev 또는 이름 나경일 → pm / 그 외 → admin)으로 임시 판정하고, 명시 tier가 하나라도 생기면 파생 중단. 부트스트랩 여부는 회원 전수를 본 쪽이 판정 — 서버 `push.tierCtx()` = `tier.ctxOf(회원)` → {bootstrap, tierOf(m), bossIds, pmIds, adminIds}(스캔 1회, 재직자만) / 앱은 `member_list`의 tier로 `tierBootstrap()`.
- **배포 직후 절차(코디네이터 블롭 작업 — 필수)**: `gw_users` 스토어 `member:<id>` 레코드(JSON)에 `tier` 키를 회원 3인에게 **동시에** 지정 — 나종운(대표) `"tier":"boss"` / 나경일 `"tier":"pm"` / 나수진 `"tier":"admin"` (예: `{"id":"u…","name":"나경일",…,"admin":true,"tier":"pm"}`). 첫 명시 tier가 생기는 순간 파생이 멈추므로 한 명만 먼저 지정하면 나머지가 admin으로 떨어진다. 앱 인사 카드로 순차 지정할 때는 **나경일 pm → 대표 boss → 나수진 admin 순**(첫 저장이 pm이 아니면 409 LAST_PM — 파생 pm이 사라지는 저장이라 거부되고, 본인 등급은 본인이 못 바꾸므로 대표 계정으로). 지정 전(부트스트랩)엔 종전대로 파생으로 동작하므로 결재는 끊기지 않는다. 대표 tier가 0명이면 ③·총정리가 잠기고(폴백 없음) 총정리 크론은 스킵+감사로그 `감시`.
- **서버 게이트**: ① 전결(직접 기안 [전결 종결] `self_decide`)과 **①·② 1단계 PM 큐 결재**는 tier pm만 — 관리자 등급(admin, 예: 관리부)은 `approval_create self_decide` 403 `PM_ONLY`(직원·대표는 종전 400 `SELF_DECIDE_PM_ONLY`), `approval_decide` PM 큐 403 `PM_ONLY`(대표도 종전대로 403). ③ 대표 전결·전결 총정리 [확인]은 boss만(`BOSS_ONLY` — **boss 0명 폴백 제거**, 9/6 S2: 종전 v310 폴백은 boss가 비면 관리자 전원에게 ③·총정리를 열었다. 대표 부재 시 보류만, 앱 `apprCanDecide`도 대표 큐=boss만, 총정리 크론은 boss 0명이면 스킵+감사로그 `감시`). 관리자 등급의 ② 기안은 PM 단계 자동통과 없이 PM 큐로(자기 결재 단계가 없으므로). `push.pmIds()`=tier pm만(결재 요청 수신·`pm_present`), `push.bossIds()`=tier boss만. pm이 0명이면 종전 폴백(PM 큐를 관리자 전원에게 — LAST_PM 전면 보호로 통상 도달하지 않음). `push.bossIds/pmIds/adminIds`는 재직자만(퇴사 제외, 종전 동기 `push.isBoss/isPm/tierOf` 제거 — 회원 한 명만 보고는 부트스트랩을 알 수 없어 게이트가 파생값을 믿게 된다).
- **관리자 등급이 유지하는 것**: 모듈 do·문서함 관리(즉시 등재·첨부 삭제·휴지통)·회원 관리·권한관리·감사로그 등 기존 admin 기능 전부. 결재는 [기안]으로 상신만(PM 큐) — [전결 종결] 버튼 미노출(`apprDraftIsPm`=tier pm), 뱃지·한장씩 큐에서 PM 큐 건 제외(`apprCanDecide`).
- **등급 변경**(인사 카드 "관리자 등급" 행 — 관리자 직책일 때만 표시): `member_upsert tier` — 변경은 요청자 tier boss·pm만(403 `TIER_PM_OR_BOSS_ONLY`), 대상은 관리자만(400 `TIER_NOT_ADMIN`), 값 검증(400 `BAD_TIER`), **마지막 pm 보호 전면**(409 `LAST_PM` — tier 강등·삭제뿐 아니라 admin:false·leave_date 경과·이름/직책/dev 변경 등 **모든 회원 저장**의 결과 재직 유효 pm이 0명이 되면 거부(9/6 S3). 이미 0명이면 막지 않음 — 지정 저장 자체가 불가능한 교착 방지), 관리자 해제 시 등급 제거. 감사로그 `등급변경`(**유효 등급**이 바뀐 모든 저장 — 파생·퇴사·해제 포함, 전→후) + 회원 수정 f.tier. **회원 저장 게이트(9/6 S1)**: 자기 계정의 name·role·admin·dev·tier 변경 403 `SELF_CHANGE_FORBIDDEN`(개발자 예외 없음 — 다른 개발자·대표가 바꾼다. 인사 정보는 본인도 저장 가능) / 이름·직책 **변경**은 개발자만(`DEV_ONLY` — 같은 값 동봉은 변경 아님) / role '대표'·예약 이름(나종운·나경일) 지정은 tier boss 또는 개발자(dev 플래그)만(403 `ROLE_BOSS_ONLY`·`NAME_RESERVED`) / 기존 회원과 같은 이름 409 `NAME_TAKEN`(목록 NFC·소문자 비교 + 이름 색인 — 이름 로그인 색인 탈취 차단) / role은 문자열만(객체는 변경 없음, R7). 명부·회원 관리·권한관리에 등급 배지(대표/PM/관리자). `member_list`에 tier·dev 동봉(비관리자 표시용).

**문서함 휴지통**: 삭제는 소프트 삭제(del:1) 그대로, **삭제 전 확인 창**(제목·번호·첨부 수 + 복구 안내). 삭제 시 `deleted_at`·`deleted_by` 스탬프(서버 `docDelStamp` — **신규 del 전환은 관리자·비관리자 모두 서버 시각·서버 by 강제**(클라 값 폐기, 9/6 S4), 낡은 사본 재저장은 서버 원본 스탬프만 이월, 비관리자 저장으로는 복구 불가). **부활 차단(9/6 R1)**: 서버 원본에 없는 id가 del:1로 오면 관리자·비관리자 저장 모두 폐기(감사로그 `제거` — 직원 경로의 '대기'+등재 카드 부활 차단) + 앱 `mergeDocs`도 remote에 없는 del:1 local 항목 제거. **비관리자 저장 재구성(9/6 S5)**: 보이는 기존 문서가 페이로드에 없으면 서버 원본 유지(누락≠삭제), del:1 전송은 본인 등재 문서만(타인 문서의 del은 무시하고 내용 편집만 반영). 관리자 문서함 맨 아래 **"🗑 휴지통" 서랍**(del===1 항목, 삭제일·삭제자·경과일, `hidden_tmp` 구건은 "임시 숨김(v314)" 배지, 검색 중엔 숨김) — 행마다 [복구]·[영구 삭제](**30일 경과 건만 활성**). 삭제일 = deleted_at → 없으면 updated_ts(구건 del:1) → updated 일자(`docDeletedTs` 앱·서버 동일).
- **서버**: `doc_restore`(관리자, del·hidden_tmp·deleted_* 제거 + restored_by/at, 감사로그 `복구`) / `doc_purge`(관리자, `confirm:true` 필수 `NEED_CONFIRM`, 삭제 아님 `NOT_DELETED`, v314 임시 숨김(hidden_tmp) `HIDDEN_TMP`(복구만 — 앱 [영구 삭제] 버튼 비활성, 9/6 R3), 30일 미경과 `PURGE_TOO_EARLY`+days_left, 삭제일 미상 `PURGE_DATE_UNKNOWN` fail-closed, 항목 제거 + 첨부 블롭 `docatt:<id>:1..att_seq`(고아 포함) 삭제, 감사로그 `영구삭제`(첨부 블롭 수)). 둘 다 낙관락 `base`(409 `STALE_BASE`)·cid 멱등(`docop:<cid>` — 7일 지난 키는 다음 휴지통 작업 때 정리, 회당 100키·실패 무시, 9/6 R6)·버전 링 스냅샷·쓰기 후 1회 자가복구. **자동 영구 삭제 없음**. 클라 [영구 삭제]는 2단 확인(내용 confirm → '영구삭제' 입력).

**기안 참조 문서 선택**: 드롭다운(`<select#apprDraftRef>`) 대신 **검색 입력**(번호·제목·분류명 — 즉시 필터) + 그 아래 **접힘 목록**(대분류 › 중분류 › 문서, 문서함 서랍과 같은 순서·번호순). 검색어가 없으면 목록 그대로(서랍 탭으로 펼침), 타이핑하면 걸러지고 일치 서랍 자동 펼침. 항목 탭=선택(칩 표시·✕ 해제, 같은 항목 재탭=해제), 기본 "없음". 선택값은 숨은 `#apprDraftRef`(문서 id) — **저장 형식 `ref doc:<id>` 종전 동일**. ES5·폰 화면(스크롤 상자 220px).

**화관법 실패 안내 문구 회수**: 로컬 폴백(김과장 PC 재시도)은 9/6 폐지 — 앱 자동 제출 카드의 실패 줄과 워커 실패 푸시 본문에 "그룹웨어 자동 복구(08:20) 또는 관리자 확인"으로 안내(재제출 부추김 없음). `hwakwan:lastrun`에 실패 요약(`error`) 기록. 9/6 실측: 앱 레포 코드에 "김과장 PC" 문구는 없었고(주석 1곳뿐) 실패 카드에 안내 자체가 없던 것이 실태 — 문구 신설이 회수의 실체.

## 5. 돈 흐름 (청구→수금→기성)

청구 단계 승인 → "기성(미수) 등록할까요?"(승인권자에게) → 기성·수금 탭에 청구 건 자동 생성(계약 연결) → 관리자가 계산서 발행 체크 → 입금 처리 → "수금확인 단계 완료할까요?" → 계약 종결. 계약 카드에 💰수금완료/미수 칩.

**세금계산서 목록 업로드**: 홈택스·**위하고** 양쪽 포맷 자동 인식(2행 머리글·그룹 컬럼 구조). **공급자 사업자번호로 시행 회사를 행별 자동 판별**(506-81-48535=환경 / 506-81-84827=건설 → 파일 단위 수기 선택 불필요, 기존 미지정 건도 재업로드로 소급 교정), **입/출구분 매입분 자동 제외**. 승인번호(신고번호) 기준 대사 — 신규 추가·금액 변경 갱신·취소분 숨김, 파일 내 중복은 별도 항목으로 노출(사람 확인). 위하고 실물 74건으로 회귀 검증(금액 정합·날짜·회사판별 100%).

**회사 구분(세무)**: 기성 건마다 환경/건설 칩. 수기=선택, 업로드=사업자번호 자동, 계약 연동=두 회사 규칙 자동. **[📤 세무 내보내기(월)]** — 월별 매출 장부 xlsx(회사별 소계·총계, 공급가액·세액·승인번호 포함, 앱 값 그대로·추정 계산 없음) → 세무사 전달용.

**실적 DB**: 종결된 계약이 자동으로 실적. 계약 탭 [실적 다운로드] → 공사명·발주처·공종·요소·금액·착공/준공·수금일·시행회사·연면적 엑셀(실적증명·기술평가 근거, 로드맵 P9).

## 6. 입찰 레이더

소스 10+1(나라장터·수자원·국방·누리·순환·K-apt·LH·철도·한수원·포스코·온비드) 평일 아침 자동 수집(GitHub Actions). 공고문 자동 판독(하한율·예가범위·A값 명기분만) → 낙찰하한가 계산기·투찰범위 칩. 개찰결과 자동 확인(낙찰/유찰), 낙찰 투찰률 통계 축적. 패스 학습(같은 기관+유사 제목 재공고 자동 패스). **발주처 실측 칩**(v227): 공고 카드·상세 기관정보에 그 발주처의 최근 낙찰 투찰률 중앙값(경북 상세는 Q1~Q3 포함, 표본 3건 이상만) 표시 — 중앙값 89.5% 미만은 ⚠(저가 경쟁 발주처, 공고문 하한율 필수 확인).

## 7. 인사·차량·기타

- **인사·직원**(관리자): 직원 명부·연차(소멸·이월 없음)·권한관리·감사로그
- **인사·일용직**: 석면·철거 투입 인력 명부(이름·생년·직책·연락처·검진일) → 해체계획서 명단 자동
- **차량**: 만기(검사·보험) 관리·점검/수리/현장출동 이력(캘린더 🚚 연동)·엑셀 다운로드(관리자)·취득가액/무자료금액(관리자 전용, 서버 차단)
- **인허가 3층 구조 1차**(v304, 시안 20260901): 서브탭 [허가증 N | 의무 대장 N | 이번 달]. ①허가증=현행 licenses+석면 대장 그대로 ②의무 대장=주기의무 등재표(2026-08-25 조사+증보 1, 95행 — 본표 73+건폐법 세트 11+비산먼지 6+특정공사 5) — 인허가별 그룹·1줄 요약(탭=상세: 기산점·근거·선행확인)·확정도 뱃지(확정/조건부)·단위 뱃지(차량/개인)·검색. 관리자 [선행확인 입력]: 답+기준일(base_date) 채우면 확정 전환(confirmed_by/at 기록, 사람 전환분만 회수 가능). 시드는 자동 주입 금지 — 대장이 비었을 때 관리자 [등재표 불러오기] 명시 1회 액션(data/duties_seed.json ← tools/duties_seed.mjs 파서로 재생성, 손 편집 금지). 저장은 licenses 문서 top-level `duties` 배열(컬렉션 신설 없음, no 키 병합) ③이번 달=2차 안내+확정도 통계. 회차 인스턴스 자동생성·persons 명부=2차, 차량단위 연동=3차(TODO 주석)
- **휴가**: 신청→승인, 캘린더 연동. **캘린더**: 만기·지시·청구·휴가·차량 현장 통합

## 8. 운영·안전장치

- **백업**: 매일 05:30 Blobs 전체 → appdata 저장소 git 스냅샷(gw_backup.json). git 이력=시점별 복구
- **증명서 갱신 루틴**: 20일 주기 자동 지시(납세·지방세·4대보험 완납증명 발급→보관함 업로드). 유효기간 30일+행정처리 지연 감안 주기. 등기부등본은 인정기간 내 재사용·만료 시에만 유료 발급, 인감증명서는 무인발급기/등기소(온라인 불가)
- **시점 복구**(관리자, 인사·직원 탭 [⏪ 시점 복구]): 컬렉션(지시·계약·기성·거래처·차량·인허가·문서·휴가·견적서)별 서버 버전 링에서 버전 선택 → 건수 미리보기 → 2단 확인("복구" 입력) 후 되돌리기. 저장할 때마다 직전 상태가 자동 보존(사람 저장 매번, 봇 수집 하루 1개 / 최근 20개+하루 1개×30일), 복구 직전 상태도 자동 보존되어 복구 자체를 되돌릴 수 있고 실행은 감사로그에 남음. 개인업무(mytasks)는 개인별 저장이라 대상 아님. 미수 장기화(청구 45일 경과) 자동 지시+푸시 포함
- **저장 단일 원천(S1)**: 모든 컬렉션 읽기·쓰기가 서버(gw-data/Blobs) 경유 — 권한·감사로그·낙관적 락·기기승인이 전 컬렉션에 강제. 클라이언트에 GitHub PAT 없음(구 jw_app_token은 부팅 시 자동 삭제). appdata 리포의 data/*.json은 스테일 미러(원천 아님), 살아있는 건 data/backup/ 일일 스냅샷뿐
- **판독 API**: Anthropic 키(Netlify env), 크레딧 선불+자동충전 꺼짐=피해 상한. 문서당 수십 원
- **퇴사자 차단**(S2-A): 퇴사일(leave_date)이 지난 계정은 로그인·기존 세션·로그인 이름목록 전부 차단(당일까지 허용, KST). 삭제(del)와 달리 인사 기록은 보존 — 사직 승인이 퇴사일을 자동 기록하므로 별도 조치 불필요
- **직원 홈**(S2-B, v228): 비관리자 홈은 "오늘 내 것" 한 판 — 내게 온 지시(완료=승인제 그대로)·이번 주기 체크(내 담당, 승인 흐름 그대로)·내 할 일(추가·완료·삭제)·휴가 신청(잔여 연차)·캘린더. 권한 숨김 모듈은 홈에도 안 나타남. 관리자 홈(파이프라인 보드)은 무변경. "업무" 탭은 **"내 업무"**로 개명, 권한으로 탭이 다 사라진 사이드바 그룹은 제목·구분선까지 숨김
- **권한관리 매트릭스**(S2-C, v228): 사람×모듈 표 — 셀 클릭마다 수행→보기→숨김 순환, 사람 단위 1초 디바운스 자동 저장(perms만 부분 전송·실패 시 1회 자동 재시도). 관리자 행은 "모든 권한 고정"
- **청구 검수(간이 게이트)**(v233~234, 용어집 결정 B): 발행 전 청구(invoice=false)에 "검수 대기/검수 ✓" 칩 — 검수자=관리자 전체, 검수자·일자 기록, 칩 클릭으로 해제(확인 후). 미검수 건 입금 처리 시 경고. 관리자 홈 승인 대기함에 "청구 검수 n건". **돈 상태 3종(입금 paid·검수 reviewed·발행 invoice)은 서버 하드 차단**(v234) — 비관리자 저장은 서버가 기존값 강제 복원(신규 청구는 미입금·미발행·미검수로 시작), 콘솔 우회 무력. 분장 근거: 공무(운영부)=청구 등록·수정까지, 관리부=검수→발행→수금→미수(용어집 시안 v1)
- **PWA**: index.html 수정 시 sw.js SHELL_CACHE 버전업 필수. 배포=claude/gw-backend push → Netlify

## 9. 미구현·대기 (킵 목록 — 2026-08-04 유령 3건 청소: 낙찰률 적용·공정표 막대·서약서 양식은 완료 확인)

- 폐기물량(톤) 자동 계산 — 환산 기준 확정 대기
- ㈜종운건설 청구 계좌 등록(준공계 청구서용 — 등록 여부 확인 필요)
- 군부대 변형 서류·계약 요소 학습 자동 제안 — 실물 재료 대기
- 실전 병행 테스트·두호천 확인은 HANDOFF §5 PM 대기로 일원화(중복 기재 제거)
### v322-a (9/7) — 헤더·운반일지 표
- 헤더 사명 `종운환경 · 종운건설`은 한 줄 고정(말줄임), 640px 미만에서는 숨기고 로고만(폰 7~8줄 세로 줄바꿈 해소 — PM 9/7). 840~860px 사이 PC 창은 그대로.
- 운반일지 탭 `#abSheetCard`: 올바로 자동 목록을 엑셀 운반내역 양식(L·R 두 표 — 행선지 상차지·하차지 / 품 목 / 회수)으로 렌더(`abSheetTableHtml`). 회수 0은 빈칸, 잠정·품목확인·단위미상은 회수 칸 아래 작은 글씨. 노선표 없으면 종전 목록 폴백. 미매칭·합계·대표 결재 버튼은 그대로.
### v323 (9/7) — 노선 지정 품목 400자·운반일지 빈 줄 숨기기/노선 숨김·로그아웃·노선 지정 운영부 허용 (PM 9/7 #9·#10·#12·#13)
- **노선 지정 길이 상한(#10)**: 올바로 법정 품목명(예: "그 밖의 폐광물유[아스팔트유·그리스(grease)·방청유 및 …]")이 80자를 넘어 `STR_TOO_LONG`으로 지정이 막히던 것 — 서버 `MAX_STR` 80→**120**(상차지·하차지), 품목은 별도 `MAX_ITEM`=**400**(학습·직접 추가·단골 노선 3곳 공통). 저장 레코드는 원문 그대로(절단 없음 — 중복 판정 `learnKey`는 normItem 정규화라 길이 무관). 화면 안내 "상차지·하차지는 각 120자, 품목은 400자, 비고는 200자"(uismoke가 서버 상수와 대조).
- **노선 지정 권한(#13)**: `ab_learn`은 **관리자 또는 부서(dept)가 '운영부'인 회원**만(서버 `canLearn` — 그 외 403 `FORBIDDEN`, 거부도 감사로그 `노선지정거부`에 by·부서 기록). 앱 `abCanLearn` 같은 조건으로 [노선 지정] 버튼 노출 — 비대상 회원은 미매칭 목록만 보고 버튼 없음("노선 지정은 관리자·운영부(공무)가 합니다"). 직접 추가·단골 노선은 종전대로 전 직원. 노선 숨김은 관리자 전용.
- **운반내역 표 빈 줄(#9)**: ① 표 위 "올바로 자동 · 조회 전용" 줄 우측 토글 **[빈 줄 숨기기]**(기본 꺼짐, 이 기기 localStorage `jw_ab_hide_empty` 기억, try/catch) — 켜면 회수가 빈 줄(잠정도 없음)을 그날 화면에서만 뺀다(서버·집계 무관), 표 아래 "빈 줄 n개 생략". ② 관리자에게 빈 줄마다 작은 **[숨김]** 버튼 → 서버 `ab_route_hide {side,row,hide}`(관리자 전용 `ADMIN_ONLY`, 실재 줄만 `BAD_ROUTE`, 불리언 검사 `BAD_INPUT`, 같은 상태 반복은 `changed:false`로 저장·감사 없음) → 블롭 `allbaro:routes_hidden {schema:1, items:[{side,row,by,bid,ts}]}`, 감사로그 `노선숨김`/`노선숨김해제`. `ab_status`의 `routes`에 `hidden:true·hidden_by·hidden_ts` 병합(`hidden_error`=숨김 블롭 읽기 실패 표시 — 그때는 숨긴 줄이 다 보이는 쪽으로 실패하고 표 아래 안내). 숨긴 줄은 표에서 빠지고 표 아래 **"숨긴 노선 n개 · [보기]"** → 목록(누가·언제)과 관리자 [복구]. **소프트 숨김** — 노선표 상수(`ROUTES`/`AB_ROUTES`)·집계·학습 배정은 무수정. **숨긴 줄에 인계서가 배정되면**(집계 count가 그 줄에 붙으면) 표에 다시 나타나되 노란 배경 + "숨김 해제 필요" 배지(관리자 [해제]) — 데이터 손실 방지. 미매칭 [노선 지정] 셀렉트에서는 숨긴 줄을 맨 아래 "숨긴 줄" 그룹에 "(숨김)" 표기로 유지(배정 가능).
- **로그아웃(#12)**: 상단 우측 이름 버튼(또는 ⚙) → 로그인 모달 맨 아래 **[로그아웃]**(로그인 상태에서만 표시) → 확인 창 → `gwLogoutCore()`: 밀린 운반일지 직접 추가 저장 flush → `setGwToken(null)`·`curMemberObj=null`·오프라인 부팅용 `jw_member_cache` 제거·**메모리 컬렉션 전부 비움**(`clearSessionData` — 지시·차량·기성·인허가·의무대장·문서함·계약·휴가·입찰·부동산·견적·홍보·거래처·내 할 일·회원·결재·가족친화·교육·주기 체크 기록·운반일지 상태. 9/6 보안 리뷰: 로그아웃 뒤 이전 관리자 배열이 남던 것) → 리로드(토큰 없음 → 로그인 화면. 반투명 모달 뒤 잔상 차단, lockLogout과 같은 처리. 저장 요청이 나가는 중이면 1.5초 뒤). **화면잠금 PIN(`jw_lock_pin_<회원id>`, 기기 해시)은 유지** — 같은 사람 재로그인 시 재세팅 없음. 기기 승인 대기 모달의 [다른 계정으로 로그인]도 같은 `gwLogoutCore` 재사용(배열 비움 포함, 리로드 없이 종전대로 모달).
- 검증: servertest 절 30(gw-allbaro 첫 편입 — 품목 200자 200·401자 400·상차지 121자 400·120/400 상한 200·운영부 200·타 부서/무부서 403·미승인 기기 403 / ab_route_hide 직원·운영부 403·L999/X 400·불리언 400·hide 200+블롭+감사·중복 changed:false·status hidden 병합·숨긴 줄 배정 가능·unhide·잔재 무시·읽기 실패 hidden_error) 23건(376 통과), uismoke 13) 토글 DOM·로그아웃·문구=상한·권한 동률·숨김 배선. sw v323.

### v324 (9/7) — 숨긴 노선 → 운반일지 엑셀 동기화(PM 9/7 ㄱ)
- **숨긴 노선 → 엑셀 동기화(PM 9/7 ㄱ, 후속)**: `ab_hidden_export {key}`(봇 전용 — 세션·기기승인 대신 `BIDS_INGEST_KEY`, body.key 또는 헤더 x-ingest-key, 길이 확인+timingSafeEqual, 불일치·누락 401 `BAD_INGEST_KEY`, env 미설정이면 닫힘) → `{ok, items:[{side,row}], n, updated_at}` 좌표만(by·bid·ts 없음, 노선표에 없는 잔재 제외, 블롭 읽기 실패 500). appdata `collectors/logsheet_daily.py`가 08:25 엑셀 생성 전 1회 호출(15초, 실패·형식 오류 시 경고+숨김 없이 전체 양식)해 숨긴 줄의 상·하차지·품목·회수 칸을 비워 **빈 줄로 남긴다**(양식 행 고정·40행 SUM·43행 광재 — 행 삭제 없음; 회수가 붙은 숨긴 줄은 그대로 = 앱 '숨김 해제 필요'와 같은 원칙). 새 secret 없음. servertest 절 31 (11건).
- 검증: servertest 절 31 (11건, 387 통과)·uismoke 전 항목. appdata `hidden-sync` → main 병합(logsheet_daily.py fetch_hidden·parse_hidden, generate.py 빈 줄 처리, selftest 3건). sw v324.

### v325 (9/7) — 부서 순서
- 인사·직원 탭·부서 선택의 부서 순서를 관리부 → **기술개발부** → 운영부 → 준설팀 → 폐기물팀으로(PM 9/7 "기술개발부는 관리부와 운영부 사이에"). `DEPT_LIST`와 `#meDept` 옵션 순서 동일. 데이터 무변경. sw v325.

### v326 (9/8) — 운반일지 매칭: 품목 다르면 묻는다 + R39 폐석회
- PM 9/8 "별도 품목인데 왜 안 물어보고 배정했나": 서버 `matchRouteEx`의 "후보 줄이 1개면 품목이 달라도 배정하고 `weak`(품목확인) 표시" 규칙 폐지. 이제 상·하차지 줄이 있어도 품목이 다르면 **배정하지 않고 미매칭 `ITEM_MISMATCH`**(후보 줄 동봉)로 드러내 [노선 지정] 또는 줄 추가를 받는다. 구내운송(품목칸=도착처, viaItem) 줄은 품목 비교가 성립하지 않으므로 종전대로. `weak`는 항상 false(필드·"품목확인" 표시 코드는 호환용 잔존). 앱 미매칭 사유 문구 "품목이 다름 — 같은 상·하차지 줄은 있음(노선 지정 또는 줄 추가)".
- 노선 R39 신설: (주)포스코퓨처엠 → 대화산업 · 폐석회(9/7 실측 — 종전엔 R14 분진 줄에 오배정). 양식 원본 R39가 빈 줄(SUM K5:K39 범위 안)이라 행 밀림 없음. 서버 ROUTES·앱 AB_ROUTES·appdata 노선사전.json·운반일지_양식_원본.xlsx 4곳 동일. 9/7 기록은 관리자 [수동 수집] 9/7 재실행으로 재집계.
- 검증: servertest 절 32(8건 — R39 배정·R14 분진·후보 1개 품목 불일치 ITEM_MISMATCH·품목 일치 배정·viaItem 예외·NO_ROUTE·ROUTES 중복 없음·앱 상수) · uismoke. sw v326.

### v327 (9/8) — 운반일지 자동 재정렬
- **무엇이**: 저장된 일자 집계(`allbaro:day:<날짜>`)를 현재 노선표·학습 사전으로 **다시 판정**한다 — 서버 `rematchDoc`(_lib/allbaro.js): counts 각 묶음의 route/weak/reason/candidates/learned만 갱신하고 `unmatched[]`를 route:null 묶음으로 재생성(aggregate와 같은 규칙). **n·qty·잠정·제외·차량합계·total은 불변.** 노선표 해시 `ROUTES_VER`(side/row/from/to/item 이어붙인 sha1 앞 12자)를 문서 `routes_ver`에, 시각을 `rematched_at`에 기록. 워커(gw-allbaro-run-background)도 저장 시 `routes_ver` 스탬프.
- **언제(트리거)**: ① `ab_learn` 성공 직후 같은 요청 안에서 — 앱이 보던 날짜(body.day)부터 오늘까지(없으면 오늘 포함 최근 7일). 응답 `rematched:{days,changed,left}`. ② `ab_status`·`ab_day` 읽기 경로에서 읽은 문서의 `routes_ver`가 현재와 다르면(없으면) 그 자리에서 재정렬·저장하고 재정렬된 값으로 응답 — ab_status는 최신순 최대 7일·4초 안, 못 한 날짜는 `stale_days`; ab_day(단일 날짜)는 항상 처리(응답 `rematched.changed`). 시간 가드: 학습 훅 6초 — 초과분은 `left`로 드러내고 현황·열기에서 마저 정리. 학습 사전을 못 읽으면 재정렬하지 않고 `stale:true`(학습 배정이 풀린 문서를 저장하는 쪽으로 실패 금지). 감사로그 `재정렬 <날짜> 변경 n건 (사유: 학습|노선표)`(변경 있는 날만, 실행당 1항목).
- **규칙**: 차량으로 갈리는 묶음(`vehicle_type` 있음)은 종전 차량 종류로 먼저 좁히고(차량이 학습을 이김) 못 좁히면 학습 순, 그래도 안 되면 **종전 줄을 붙들지 않고 미매칭**(조용한 오배정 금지 — PM 9/8). 숨긴 노선(`allbaro:routes_hidden`)·직접 추가(`allbaro:manual:*`)·학습(`allbaro:learned`) 블롭은 손대지 않는다. 새 컬렉션·새 버튼 없음.
- **왜**: 9/8 실사고 — 수동 수집이 배포 8분 전에 돌아 옛 규칙(R14 분진)으로 집계된 9/7 기록이 다음 크론(08:00)까지 그대로였고, [노선 지정]도 "다음 수집부터"라 사람이 [수동 수집]을 눌러야 화면이 바뀌었다. PM 9/8: "학습시키거나 노선표가 바뀌면 사람이 수동 수집을 누르지 않아도 기록이 스스로 재정렬돼야 한다."
- **앱**: [노선 지정] 확인 문구 "저장 즉시 이 날짜부터의 기록이 자동으로 재정렬됩니다(다시 수집할 필요 없음)", 요청에 `day` 동봉, 성공 토스트 "노선 지정 R36 · 재정렬 n건(남은 k일은 열 때 정리)", 응답 후 그날 재조회. 표 위 작은 안내 "노선표 변경 뒤 재정렬 대기 n일(열면 자동 정리)"(`stale_days`, 날짜를 열면 목록에서 빠짐). "다음 수집부터" 문구 회수.
- 검증: servertest 절 33(옛 규칙 블롭 ab_day 재정렬 R14→R39·routes_ver·n/qty/total 불변·감사 / ab_learn(day) 그날 묶음 학습 줄·rematched / 무관 묶음 불변 / 차량 분리 묶음 같은 줄·차량 미상은 미매칭 / 숨김·수동 블롭 무변경 / 시간 가드 left / routes_ver 같으면 저장 0 / 학습 못 읽으면 stale·저장 0 / ab_status 7일 상한·stale_days / BAD_DAY / 순수성) · uismoke 14). sw v327.
