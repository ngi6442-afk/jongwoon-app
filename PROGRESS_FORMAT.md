# PROGRESS_FORMAT.md — 진척도·체크리스트 표시 규격 v2 (PM 확정 2026-08-06)

> PM이 "진척도/체크리스트 띄워봐"라고 하면 **반드시 이 3층 드릴다운**으로 낸다. 가장 넓은 층이 맨 위.
> 목적(PM 원문 취지): 이 틀을 매번 같은 모양으로 확인해서 **벗어나는 행위를 줄이고, 그때그때 위치를 확인**한다.
> v1(2026-08-04, 플랫폼 도메인 9개가 1단계)은 이 문서로 대체 — 도메인 9개는 2층(업무플랫폼의 차상위)으로 내려감.

## 1층 · 최상위 — 큰틀 4개 + 상시 레인 1개 (고정, 바꾸려면 PM 합의)

| # | 큰틀 | 정의 |
|---|---|---|
| 1 | 업무플랫폼 | ERP 도구를 만드는 일 전부 (수주 시스템 포함) |
| 2 | 인증·자격 | 회사 자격을 취득·유지하는 활동 (연구전담부서·위험성평가·안전성평가 등) |
| 3 | 홍보·검색노출 | 검색 등록·홍보모듈 운영·홈페이지 |
| 4 | 경영 기반 | 규정·용어·상장요건·조직 제도 |
| — | 공사·입찰 실무 (상시) | 건별 발생하는 실제 응찰·서류·현장 행정. **진척도 없음** — % 대신 "진행 중 건 n건"으로 표기 |

- 각 큰틀에 % + 한 줄 요약. 맨 위에 큰틀 4개 평균(추정 명기).
- 경계 주의: 도메인 8 "실적·자격 DB"의 **기능 개발**(판독·자동반영·입력 화면)은 큰틀 1, **인증 취득 활동 자체**(신고·심사·문의)는 큰틀 2. 8/4 "별도 대장 금지" 결정은 대장 문서를 만들지 말라는 뜻이며, 진척도 층위에서 큰틀 2로 보는 것은 PM 확정(8/6).

## 2층 · 차상위 분류별 진척도

- 큰틀 1 = 기존 도메인 9개 그대로 (데이터·보안 / 운영 모듈 / 인사·권한 / 계약·서류 / 돈 / 수주 레이더·계산기 / 디자인·UX / 실적·자격 DB / 직원 실사용).
- 큰틀 2 = 연구전담부서(KOITA) / 위험성평가 인정(KOSHA) / 안전성평가 등급 / (보류) 벤처확인.
- 큰틀 3 = 검색 등록(소유확인) / 홍보모듈 정착 / 홈페이지.
- 큰틀 4 = 용어·규정 / 상장요건 / 조직·인사 제도 / 문서 체계(jongwoon-docs).
- 각 분류는 바 + % + 남은 것 한 줄. 병목(코랄)이 눈에 띄게.

## 3층 · 세부 체크리스트

- **초점 큰틀만** 펼친다(기본 = 병목 또는 오늘 임계 경로가 속한 큰틀, PM이 지정하면 그 큰틀). 전 큰틀 전개는 요청 시에만.
- 형태: ☑/☐ 체크리스트 + 현재 항목 "지금 여기" + 담당(PM/AI/외부).

## 규칙 (v1에서 유지)

- **색 = 의미**: teal ≥85 / amber 50~84 / coral <50. 3계열만.
- **병목이 주인공** — 요약 문장은 "무엇이 끌어내리는가"로 시작.
- **수치는 추정 명기** + 산정 근거 한 줄. 정밀화 요청 시에만 전수 카운트.
- 렌더링(2026-08-06 PM 확정, B안): `Desktop\작업\00_진척도\`의 **HTML 6종**이 기본 — 진척도_큰틀.html(허브)·상세 4종 + **wallpaper.html**(다크·우측 패널 640px·시작점 36vh 고정(아래로만 성장)·클릭 시 같은 페이지 안에서 층 전환·90초 자동 reload). 벽지 층 구성: 홈=큰틀 4바+**전체 가중치 상위 6** / 2층=분류 바+**그 큰틀 가중치 상위 5**(번호·담당 표기) / 3층=분류별 완전 상세(버전·경로·금지사항 포함). 가중치 기준: 마감 임박 > 병목 해소 관문 > 외부 대기 추적 > 장기. wallpaper.html은 **Lively Wallpaper**(설치됨)가 바탕화면에 라이브 렌더. "진척도 갱신해" 시 HTML 6종만 다시 쓰면 벽지 자동 반영. 벽지_진척도.png(+벽지생성.py)는 Lively 꺼졌을 때 정적 폴백.
- Lively 연결 세부(재설정 시 참고): 라이브러리 항목 `%LOCALAPPDATA%\Lively Wallpaper\Library\wallpapers\jinchokdo.claude\LivelyInfo.json`(Type 1, FileName=`C:\jw-progress\wallpaper.html`, IsAbsolutePath true — `C:\jw-progress`는 00_진척도로의 정션). **주의: WallpaperLayout.json의 LivelyInfoPath는 json이 아니라 폴더 경로, 화면 키는 `Display`**(v2.2.1 스키마 — LivelyScreen·json경로로 쓰면 "LivelyInfo.json not found"로 조용히 실패). 대화 내 위젯은 보조.
- 갱신 원천: FEATURES.md + HANDOFF.md §5 — 어긋나면 그 둘이 맞다. 인증·홍보·경영은 메모리와 Desktop\작업\01~13 폴더 산출물 기준.

## 현재 기준값 (2026-08-06 오후 — 갱신 시 이 표를 고칠 것)

### 1층
| 큰틀 | % | 한 줄 |
|---|---|---|
| 업무플랫폼 | 74 | 직원 실사용(30)이 끌어내림 — PIN 배포가 관문 |
| 인증·자격 | 55 | 연구전담부서 제출 직전(85), 위험성평가 착수 전(20), 안전성 재평가 대기(60) |
| 홍보·검색노출 | 60 | 소유확인 토큰 대기 + 홍보모듈 권한·GPS 검증 대기 |
| 경영 기반 | 50 | 용어 확정(90) 외 상장요건(15)·제도 문서화 초기 |
| 상시: 공사·입찰 실무 | — | 건별 표기 (안전성 재평가 결과 대기 1건 등) |

### 2층 — 큰틀 1 (v236 기준 + 8/6 보정)
데이터·보안 98 / 돈 90 / 운영 90 / 인사·권한 90 / 계약·서류 85 / 수주 80 / 디자인·UX 60 / 실적 DB(기능) 40 / 직원 실사용 30

### 2층 — 큰틀 2~4 (8/6 추정)
KOITA 85 · KOSHA 20 · 안전성 60 · 벤처 보류 / 검색등록 40 · 홍보모듈 70 · 홈페이지 80 / 용어 90 · 상장 15 · 조직제도 60 · 문서체계 55
