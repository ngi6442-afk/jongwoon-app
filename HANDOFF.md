# HANDOFF.md — 세션 간 운영 헌장 (모든 AI 세션은 작업 전 이 파일을 정독한다)

> 목적: 어느 기기·어느 세션에서 작업하든 ⓐ이전 작업 롤백 ⓑ금지 행위 ⓒ이미 해제된 금지를 금지로 오인 — 을 막는다.
> **규칙이 바뀌거나 금지가 해제되면 그 변경을 만든 커밋에서 이 파일을 함께 갱신한다**(살아있는 문서).
> 최종 갱신: 2026-08-03

## 0. 기본 사실 (오해 금지)
- **배포 = Netlify가 `claude/gw-backend` 브랜치를 배포**한다. main은 참고용 미러일 뿐. 앱 작업은 반드시 gw-backend 기반.
- index.html 수정 시 **sw.js SHELL_CACHE 버전업 필수**(현재 vNNN — sw.js 1행 확인).
- 라이브 데이터 = **Netlify Blobs**(회원·권한·템플릿·증빙·푸시·입찰). jongwoon-appdata의 `data/*.json` 중 users·tasks 등은 **구식 시드**이며(라이브 아님), bids·awards·health만 수집봇이 계속 갱신한다.
- 컬렉션 저장은 낙관적 락(base/409) — v215. 서버 권한 강제.

## 1. 금지 (전 세션 공통 — PM이 대화로 해제하지 않는 한 유지)
- 사용자의 실제 Chrome(claude-in-chrome) 사용 금지. 사용자 PC에 다운로드 창을 띄우는 행위 금지.
- **추측 금지**: API 엔드포인트·필드명·산식·요율은 실측(탐침·실물·오피셜 원문) 확인 전에 코드로 쓰지 않는다. 화면에 내는 숫자는 단위검증부터.
- 법령·수치는 오피셜 원문만(law.go.kr·실제 공고·실물 서류). 업체 블로그 요약 인용 금지.
- 커밋 메시지에 큰따옴표(") 금지 — PS 인자 깨짐. ps1 파일은 UTF-8 BOM 필수.
- 작업 완료 후 브라우저 미리보기 금지(검증은 node 문법검사·회귀로).
- 중국산 SW 설치 금지. 스펙·설계 임의 변경 금지(백업 복원 방식 임의 변경 사고 있었음 — PM 승인 후).
- 기존 기능 제거·롤백은 **아래 §3 의도된 설계 대조 + PM 승인** 없이는 금지.

## 2. 해제된 금지 (금지로 오인하지 말 것 — 조건 명시)
| 항목 | 상태 | 조건 |
|---|---|---|
| 한컴 COM 무인 자동화 | ✅ 허용 | 공식 보안모듈 DLL(RegisterModule FilePathCheckDLL) — **데스크탑에만 설치됨**. 타 기기는 보안창이 뜨므로 hwp 실기 검증은 데스크탑에서 |
| 국가철도공단 개찰 페이지 파싱 | ✅ 허용 | robots.txt Allow·약관에 자동수집 금지 없음 확인(2026-07-30). 요청 간 0.4초 유지 |
| GitHub Actions dispatch | ✅ 허용 | gh CLI 없음 → `git credential fill`(파일 리다이렉트)로 PAT 취득 후 REST |
| 시드 users.json PIN 공란 | ✅ 의도 | 평문 PIN 노출 스크럽(2026-07-30). 라이브는 Blobs scrypt 해시 — 시드에 PIN 다시 넣지 말 것 |

## 3. 의도된 설계 (버그로 오인해 "고치지" 말 것)
- **이윤 요율이 15%가 아닌 미세값**(0.14999994 등): 실물 관행 — 목표 도급액 안착을 위한 단수 조정. 낙찰률 정밀 안착·정산 엔진이 같은 기법.
- **≈ 표시 추정값**(하한율 최빈값·A값 공개합산): 의도된 추정 표시 정책. 지우거나 확정값처럼 바꾸지 말 것.
- hwpx 채움의 **lineseg 국소 프루닝**(토큰 문단만 1줄 축소): 전역 축소는 렌더 뭉개짐(실측). 건드리지 말 것.
- xlsx 채움에서 **수식 셀 skip 가드**: calcChain 고아 방지. 발주자 수식 셀(예: 착공계 B26)은 공란화 금지.
- 차량 회색 상태 = `말소예정·매각·검사보류`(앱 isGreyState와 서버·크론 동일해야 함).
- 적격 통과선의 가정 문구(실적 만점·관할 소재 등)는 화면에 반드시 유지.
- 계약 단계 [서류]: 공문서·낙찰=con_docs / 개인 사문서=계약서 작성 툴 (v208).
- tasks 컬렉션 쓰기 전원 허용은 승인제 성립을 위한 설계(서버 주석 참조).

## 4. 검증 의무 (push 전)
1. 인라인 JS: `node -e "...new Function..."` 문법 통과
2. **회귀 5단계**: `powershell -File tools/regress.ps1` — 서류 12종 + 세금계산서 파서 + 정산 엔진. 한컴·엑셀 없는 기기는 최소 `node tools/regress_fill.mjs` + rectest + settletest
3. sw.js 버전업 + FEATURES.md 동기(기능 변경 시)
4. 서버 함수: `node --check netlify/functions/*.js` / 파이썬: `python -m py_compile`

## 5. 현재 큐 (이어받을 것 — 완료된 걸 다시 하지 말 것)
- 진행 순서: ~~R2 낙찰 통계 화면~~(✅ v216) → ~~정산 UI~~(✅ v217 — settle_doc, F14~19·F22+L26 매핑) → ~~R3 API 경보~~(✅ appdata awards.py api0_streak) → ~~서약서 6·7 자동~~(✅ v218 — 입찰 건 해당없음 자동체크) → ~~gw-data 서버 테스트~~(✅ tools/servertest.mjs, 회귀 6단계) → ~~오류로그 뷰~~(✅ v219 — 유형별 집계·회원별 횟수) → ~~P5b 실적 자동합산~~(✅ v220 — qualPerfPrefill) → ~~P5c 등급확인서 판독~~(✅ v221 — grade_parse·인허가 자동반영·만료 연동) → ~~P6 전역 검색~~(✅ v222 — 헤더 🔍, 권한 범위 내 9개 모듈) → ~~P7 대시보드~~(✅ 시안 B 채택·v223 구현) → ~~P8 스모크·TESTLIST 대조~~(✅ tools/uismoke.mjs — 회귀 0단계, 전 페이즈 마감)
- 외부 대기: 공공데이터포털 활용신청 4건(**8월 개편 후** — 15058826 LH·15129458 누리·15059177 K-apt·15057180 LH예가), LH·누리·K-apt 어댑터는 사양 확정 상태(scratchpad 아님, 커밋된 awardprobe.py 참조)
- PM 대기: 두호천 실사용 확인, PIN 변경(4명), 나경일 중복 계정 정리 답변
- 최근 배포 이력: v206(낙찰 자동오픈·conAmt 폴백) v207(통과선 등록등급 실계산) v208(계약 라우팅·중복가드) v209(A값 항상) v210(하한율 항상·≈) v211(대장 자동) v212~215(경화·낙관적 락)

## 6. 세션 시작 절차 (PM 프롬프트 한 줄이면 됨)
1. `claude/gw-backend` 체크아웃(또는 그 기반 브랜치 생성) — main 기반 금지
2. 이 파일 + CLAUDE.md + FEATURES.md 정독
3. 작업 후 §4 검증 → gw-backend push(배포됨을 인지) → 이 파일 §5 큐 갱신
