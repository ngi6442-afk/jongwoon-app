# HANDOFF.md — 세션 간 운영 헌장 (모든 AI 세션은 작업 전 이 파일을 정독한다)

> 목적: 어느 기기·어느 세션에서 작업하든 ⓐ이전 작업 롤백 ⓑ금지 행위 ⓒ이미 해제된 금지를 금지로 오인 — 을 막는다.
> **규칙이 바뀌거나 금지가 해제되면 그 변경을 만든 커밋에서 이 파일을 함께 갱신한다**(살아있는 문서).
> 최종 갱신: 2026-09-04

## 0. 기본 사실 (오해 금지)
- **배포 = Netlify가 `claude/gw-backend` 브랜치를 배포**한다. main은 참고용 미러일 뿐. 앱 작업은 반드시 gw-backend 기반.
- index.html 수정 시 **sw.js SHELL_CACHE 버전업 필수**(현재 vNNN — sw.js 1행 확인).
- 라이브 데이터 = **Netlify Blobs**(회원·권한·템플릿·증빙·푸시·입찰). jongwoon-appdata의 `data/*.json`은 **전부 스테일 미러**(라이브 아님) — 살아있는 건 `data/backup/` 일일 스냅샷뿐.
- 컬렉션 저장은 낙관적 락(base/409) — v215. 서버 권한 강제.
- **단일 원천(S1, v224~226)**: 전 컬렉션이 서버(gw-data) 경유. 클라이언트에 GitHub PAT 없음(jw_app_token 부팅 시 자동 삭제). 시점 복구 = Blobs 버전 링(ver_list/ver_get/ver_restore) — 리포 git 이력을 다시 보게 만들지 말 것.

## 1. 금지 (전 세션 공통 — PM이 대화로 해제하지 않는 한 유지)
- 사용자의 실제 Chrome(claude-in-chrome) 사용 금지. 사용자 PC에 다운로드 창을 띄우는 행위 금지.
- **추측 금지**: API 엔드포인트·필드명·산식·요율은 실측(탐침·실물·오피셜 원문) 확인 전에 코드로 쓰지 않는다. 화면에 내는 숫자는 단위검증부터.
- 법령·수치는 오피셜 원문만(law.go.kr·실제 공고·실물 서류). 업체 블로그 요약 인용 금지.
- 커밋 메시지에 큰따옴표(") 금지 — PS 인자 깨짐. ps1 파일은 UTF-8 BOM 필수.
- 작업 완료 후 브라우저 미리보기 금지(검증은 node 문법검사·회귀로).
- 중국산 SW 설치 금지. 스펙·설계 임의 변경 금지(백업 복원 방식 임의 변경 사고 있었음 — PM 승인 후).
- 기존 기능 제거·롤백은 **아래 §3 의도된 설계 대조 + PM 승인** 없이는 금지.

## 2. 해제된 금지 (금지로 오인하지 말 것 — 조건 명시)
| 항목 | 상태 | 조건 |
|---|---|---|
| 한컴 COM 무인 자동화 | ✅ 허용 | 공식 보안모듈 DLL(RegisterModule FilePathCheckDLL) — **데스크탑에만 설치됨**. 타 기기는 보안창이 뜨므로 hwp 실기 검증은 데스크탑에서 |
| 국가철도공단 개찰 페이지 파싱 | ✅ 허용 | robots.txt Allow·약관에 자동수집 금지 없음 확인(2026-07-30). 요청 간 0.4초 유지 |
| GitHub Actions dispatch | ✅ 허용 | gh CLI 없음 → `git credential fill`(파일 리다이렉트)로 PAT 취득 후 REST |
| 시드 users.json PIN 공란 | ✅ 의도 | 평문 PIN 노출 스크럽(2026-07-30). 라이브는 Blobs scrypt 해시 — 시드에 PIN 다시 넣지 말 것 |

## 3. 의도된 설계 (버그로 오인해 "고치지" 말 것)
- **이윤 요율이 15%가 아닌 미세값**(0.14999994 등): 실물 관행 — 목표 도급액 안착을 위한 단수 조정. 낙찰률 정밀 안착·정산 엔진이 같은 기법.
- **≈ 표시 추정값**(하한율 최빈값·A값 공개합산): 의도된 추정 표시 정책. 지우거나 확정값처럼 바꾸지 말 것.
- hwpx 채움의 **lineseg 국소 프루닝**(토큰 문단만 1줄 축소): 전역 축소는 렌더 뭉개짐(실측). 건드리지 말 것.
- xlsx 채움에서 **수식 셀 skip 가드**: calcChain 고아 방지. 발주자 수식 셀(예: 착공계 B26)은 공란화 금지.
- 차량 회색 상태 = `말소예정·매각·검사보류`(앱 isGreyState와 서버·크론 동일해야 함).
- 적격 통과선의 가정 문구(실적 만점·관할 소재 등)는 화면에 반드시 유지.
- 계약 단계 [서류]: 공문서·낙찰=con_docs / 개인 사문서=계약서 작성 툴 (v208).
- tasks 컬렉션 쓰기 전원 허용은 승인제 성립을 위한 설계(서버 주석 참조). **지시 승인 위조(콘솔 우회) 가능성은 PM이 인지하고 감사로그 사후 적발로 수용**(2026-08-04 보안 감사 — "사후적발 ㄱ"). 주기 체크 자가승인·사내 푸시 임의 발송도 저위험 수용. 서버 강제로 올리려면 PM 재결정 필요 — 임의로 막지 말 것.

## 3b. 주장 등급제 (보고·감사 공통 — 판정 오보 3회 후 도입 2026-08-04)
모든 발견·판정에 등급을 단다: **[실측]**(원문 읽고 재현·대조 — 판정 가능) / **[표본]**(일부 확인 — "표본 n곳" 명시하고 잠정 판정) / **[신호]**(카운트·패턴 매칭만 — **판정 금지**, 원문 확인 후에만 승격).
실사고: "19곳 뒷문"(인터셉터 안 읽음)·"도급액/계약금액 혼용"(카운트만 봄)·견적 저장 "완료"(경로 미검증) — 전부 [신호]에 판정을 붙인 오보.
PM 지시("추천받는다"·"의견 줘")는 **제시까지만** — 실행·배포는 명시 승인(ㄱ) 후.

## 4. 검증 의무 (push 전)
1. 인라인 JS: `node -e "...new Function..."` 문법 통과
2. **회귀 5단계**: `powershell -File tools/regress.ps1` — 서류 12종 + 세금계산서 파서 + 정산 엔진. 한컴·엑셀 없는 기기는 최소 `node tools/regress_fill.mjs` + rectest + settletest
3. sw.js 버전업 + FEATURES.md 동기(기능 변경 시)
4. 서버 함수: `node --check netlify/functions/*.js` / 파이썬: `python -m py_compile`

## 5. 현재 큐 (이어받을 것 — 완료된 걸 다시 하지 말 것)
- 진행 순서: ~~R2 낙찰 통계 화면~~(✅ v216) → ~~정산 UI~~(✅ v217 — settle_doc, F14~19·F22+L26 매핑) → ~~R3 API 경보~~(✅ appdata awards.py api0_streak) → ~~서약서 6·7 자동~~(✅ v218 — 입찰 건 해당없음 자동체크) → ~~gw-data 서버 테스트~~(✅ tools/servertest.mjs, 회귀 6단계) → ~~오류로그 뷰~~(✅ v219 — 유형별 집계·회원별 횟수) → ~~P5b 실적 자동합산~~(✅ v220 — qualPerfPrefill) → ~~P5c 등급확인서 판독~~(✅ v221 — grade_parse·인허가 자동반영·만료 연동) → ~~P6 전역 검색~~(✅ v222 — 헤더 🔍, 권한 범위 내 9개 모듈) → ~~P7 대시보드~~(✅ 시안 B 채택·v223 구현) → ~~P8 스모크·TESTLIST 대조~~(✅ tools/uismoke.mjs — 회귀 0단계, 전 페이즈 마감)
- S1 단일 원천 승격(✅ v224~226 + PAT 폐기). S3-1 발주처 실측 칩(✅ v227). S2 개발분 완료: 퇴사자 차단 → 직원 홈+내 업무 개명(v228) → 권한 매트릭스(v228~229)
- **권한 최종 확정(PM, 2026-08-04 — "보정 6칸" 권고는 철회됨, 다시 꺼내지 말 것)**: 원칙="직원에겐 자기 것만". 지시·기성·수금·거래처 = **전 직원 숨김**(전사 관리 화면·돈·거래처는 관리자 3인 전용, 직원은 홈·내 업무 인박스로 자기 지시 수신·완료 — 서버 hide 예외+taskForMe 설계로 성립). 운영부(김정아·권민지)=계약·차량(·인허가) 수행, 김정아 일용직 수행. 현장=차량 보기+주기 체크 수행. 닫고 시작→필요 시 개방
- **용어 표준화 완료**(✅ 2026-08-04, v230~236): 용어집 v1.0 확정(Desktop\work\09_조사_문서\용어집_시안_v1.md — 게이트 1~3 조사+결정 A~E). 핵심: 운영부=공무(부서명 유지) / 돈 절단선: 공무=청구 등록·수정까지, 관리부=검수→발행→수금→미수 / 간이 검수 게이트 + **돈 상태 3종(paid·reviewed·invoice) 서버 하드 차단** / 탭=기성·수금 / 표시=투찰(저장값 "응찰" 유지 — 값 바꾸지 말 것) / 차량 유지. 이후 용어 변경은 용어집 기준·PM 승인으로만
- 의도된 결정(바꾸지 말 것): "내게 온 지시"가 홈·내 업무 탭 중복 표시 — 관리자는 직원 홈이 없어 내 업무 탭 인박스를 쓰므로 유지. 직원 첫 주 실사용 후 제거 여부 결정
- **온보딩 리허설 통과**(✅ 2026-08-04, 김호태 계정): PIN 발급→로그인→기기승인→권한 반영 직원 홈→지시 완료→승인제 왕복 전 구간 검증. **정정(8/6, PM 실토): 실행 주체는 PM 본인 — 실직원 검증은 아직 0건**, 첫 직원 실사용이 진짜 첫 테스트. 리허설 중 발견·수리: 견적서 탭 권한 미등록(v238 독립 모듈·기본 숨김)
- **PM 실행 대기**: 나머지 직원 PIN 발급(인사 카드) → 안내문 배포(Desktop\work\08_앱_시안\직원온보딩_안내.html) → 로그인 들어오는 대로 기기승인 → 첫 주 불편 수집. ~~E 문서함 분류별 권한은 백로그~~(✅ v314 공개범위·등재 결재 — FEATURES §4.9)
- **홍보 모듈**(✅ v240, 2026-08-05 — 검색노출 트랙): promo 탭(기본 숨김) — 현장 기록→블로그 초안 변주 생성(제목5×본문4 강제 회전)→검수→게시 추적. GPS 자동 지역(원본 화질만 생존 — 실측). PM 대기: **권한관리에서 권민지 홍보=수행 부여**, 기종별 GPS 검증(아이폰·구버전 안드로이드 원본 전송 사진 필요). 후속 킵: 사진대지 hwpx·갤러리 member-relay 연결. 배경 문서: Desktop\work\05_홍보_검색노출\블로그_작성지침_v1.md·검색등록_안내.md
- 외부 대기 → **활용신청 전건 처리**(2026-08-04): 15058826 LH·15129458 누리·15057180 LH예가 제출. K-apt 15059177(입찰결과공지)·15058166(입찰공고) 중 **하나는 신규 제출, 하나는 이미 신청돼 있었음**(포털 안내 — 이미 승인 상태면 즉시 어댑터 가능, awardprobe 탐침으로 판정 중). 승인 확인되는 것부터 어댑터 각 30분(사양은 커밋된 awardprobe.py, 공고 어댑터는 직파싱 대체)
- **포털 사건(2026-08-04)**: 공공데이터포털이 AI 기반 활용 관리 도입 — 주력 수집 API(나라장터 입찰공고정보서비스)의 활용신청이 **중지 처리됨 → PM이 변경신청 제출(승인 대기)**. 수집은 8/4 저녁까지 정상. 실패 시 R3(0건 2연속→푸시)가 감지. 반려되면 사유 확인 후 호출 패턴 조정. **수집이 죽어 있으면 이 사건부터 의심할 것**
- 차세대 나라장터 정정(2026-08-04 조사): 이주는 **이미 2025-01-06 개통으로 지나간 사건** — 현행 API 유지보수 중(2026-06 수정 이력), 중단 예고 없음. "이주 계획" 백로그는 "구 API 중단 공지 시 신규 API(15129394 계열) 전환 30분"으로 격하
- PM 대기: ~~견적서 양식 2종 재등록~~(✅ 8/4), 두호천 실사용 확인, ~~인증 트랙 확인 2건~~(✅ 8/4 — 요원=나경일 본인: 대기환경기사+지리학 학사, 겸직 문구 유의 / 업종 38120 지정폐기물 수집운반=위생및유사서비스 계열 유력·42201 공사계열 제외)
- **8/6 전수 대조 결정(PM)**: 벤처확인=전담부서 인정 수령 시 재검토(상장 문턱 절반 효과) / ISO 45001·이노비즈·고용선정제·하도급=후보 풀(착수는 지시로) / 청구 공문=보류 확정(김천 실물 양식 확보 시 재개) / 사정률 분석·투찰 포지션 v2=보류(진척도 유지) / UX 감사 #1·#6=유령 판정(로드 실패 0건·열람은 주석만 — v230~236에서 해소됨). 홈페이지 PR #302 병합분이 Netlify 잠금으로 라이브 미반영 → PM Publish 대기. 배포 이력 추가: v240 홍보 탭 / v241 PERM_ORDER 홍보 열 누락 수리+uismoke 일치 검사 / v242 퇴사자 분리. **모델 정정(PM 8/6): 계산서 발행 기능은 앱에 없고 탑재 불가(발행=위하고·홈택스 외부, 경리 몫) — 앱·서버가 강제하는 건 "발행됨(invoice) 상태 기록"의 순서(검수 없인 기록 불가)이지 발행 행위가 아님. 청구 공문은 통상 불요(청구서만 제출), 군부대 건만 불확실 — 군부대 실물 나올 때 판단
- **컴플라이언스 기록(8/7 새벽)**: 사업장폐기물 수집운반 허가는 **배출시설계 61종 전용**(허가조건 4항: 사업장생활계·건설·지정·생활폐기물 수집운반 금지 — 별지 실측). 홈페이지 "사업장비배출시설계" 탭은 허가 범위 밖이나 **사장님 오더로 유지**(문구는 "허가 범위의" 자기한정형으로 방어). 품목 추가 변경허가 논의 시 이 기록 참조. 허가증 통본 2건(지정 26.05.27·사업장)은 Desktop 작업 루트에 PDF 보관
- **인증 트랙 승인**(2026-08-04 "1,2 ㄱ"): ①연구개발전담부서(신고제 — 요원 1명·50㎡ 파티션 허용·rnd.or.kr, 세액공제는 세무사 협의) ②위험성평가 인정(KOSHA — 감독유예 3년, 보험료 20%는 건설업 제외·환경법인은 위생및유사서비스업 분류 여부에 달림) ③**가족친화인증(여가부 — 8/4 밤 "ab" 승인으로 책자 해부 완료, 2027.4 신청·실적기간=2026년, 지금 시작 5종은 대표 결재 대기 — 상세는 Desktop 작업 09_조사_문서\인증제도_전수조사.md. 8/6 누락 발견·등재)**. 벤처확인은 보류. 진척도 귀속: 실적·자격 DB 도메인(별도 대장 금지). **전담부서 신고 일시 보류(8/4 밤)**: KOITA 사전체크 6번 — 요원(나경일)이 대표 직계비속이라 고용·산재 적용 제외 추정 → 8/5 문의로 소명 경로 확정 → **8/6 밤 해소: 요원 고용·산재 이미 가입 확인(본인 진술) — 이슈 원천 소멸, 사전체크 사실대로 통과**
- **안전성평가(2026-08-04 확정)**: 법정 주기 의무 — 원문 확인 결과 **S=3년 / A·B·C=2년 / D=1년**(KOSHA 석면해체·제거작업 안전성 평가 실시 공고. PM 기억 A3/B2/C1과 달랐음 — 3년은 S등급). 현재 D=1년 주기, **재평가 결과 대기 중** — 결과 나오면 등급확인서 업로드(P5c 자동 반영). v239: 통보서에 유효기간 없으면 발급일+주기로 만료일 자동 제안 구현
- 종결(2026-08-04): PAT(JW_APP_DATA) 폐기 완료 — 남은 토큰 member-relay(홈페이지 CMS 사용 중)·AI_BUILD_GITHUB_TOKEN(로컬 개발 추정)은 건드리지 말 것. PIN 건도 종결 — 관리자 PIN은 8자로 재설정됨(서버가 관리자 8자 이상 강제), 깃 이력의 구 4자리는 죽은 값. "나경일 중복 계정"은 라이브에 없음(시드에만 2건, 하나는 del=1). 이 건들로 PM에게 다시 묻지 말 것
- **v317·v318(9/4) — 문서함 2층 분류 체계 v2**(Desktop\work\09_조사_문서\문서체계_설계안_v2_20260904.md §6 앱 반영 명세 — PM 9/4 14:20 확정, §9 12항 중 4번만 반려 → TBM일지 법인별 2본 JW-06-01-002-03 [종운환경]·-04 [종운건설], 안전점검표 -05): 1층 업무 영역 12+99 × 2층 문서 성격 6 → `cat="AA-BB"`(73키), 번호 4층 `JW-AA-BB-NNN[-SS|-YYYY]`. 앱 `DOC_MAJOR`·`DOC_MINOR`→`DOC_CATS`·`DOC_CAT_ORDER` 생성·`docCatOf` 새 정규식 `/JW-?(\d{2})-(\d{2})-\d{3}/i`(구형식·구 2자리 cat·번호 없음→99, 텍스트 폴백 삭제)·`docMajorOf`(01 하드차단 판정·설정 키=대분류, 구 cat '01'은 파생 실패해도 01)·모달 2단 select(`#docCatMajor`→`#docCatMinor`, 신규 기본 99·중분류 필수·99/01은 관리자만)·2층 서랍(빈 중분류 숨김·빈 대분류 관리자만·99 서랍 관리자만)·체크리스트 form 21행+주석 3행+placeholder 치환 / 서버 `DOC_MAJOR_LABEL`·`DOC_MINOR_LABEL`→`DOC_CAT_SET`·`DOC_CAT_LABEL`(카드 "분류 06-01 안전보건 · 규정·기준")·`docMajorOf`(docScopeOf·docVisible·save keep·신규 01 폐기)·bulk 73키. sw v317, servertest 절 19~23 fixture 2층 cat(+구 cat 01 하드차단·중분류 키 BAD_CAT·4층 파생 11건), uismoke 11) 절 교체. **이 커밋 밖(나중 단계)**: ②블롭 마이그레이션 §6.4·§7(활성 61→59, 견적서 hwp 병합 — 배포 직후, 그 사이 관리자에게 전부 99·직원 0) ③레포 개편 ④규정 v3 ⑥신규 8건 발급. CLAUDE.md 참조는 새 번호로 바꾸되 파일 경로는 레포 개편 전까지 구 경로 병기. 상세 FEATURES §4.9 말미, TESTLIST v317.
- **v315(9/4) — 직접 기안·PM 전결 종결 + 문서함 파일 첨부·일괄 등재**(PM "사규·가족친화 결재는 어딨노?" + "실물 없는 포인터 목록"): ①상단 [✍ 기안](대표 계정 숨김) 모달 — 종류(등급표 kind − 지시·운반일지·문서함 등재·휴가·전결총정리)·제목·내용·참조 문서(`ref doc:<id>`)·등급 미리보기(`approvals_list`가 `grades` 동봉). PM(비대표 관리자)+①=[전결 종결]=`approval_create self_decide:true` → 서버가 생성+승인 한 쓰기(status 승인·decided_by 본인·`self_decided`·chain `전결`·감사로그 `전결`·푸시 0·큐 미노출), 직원/대표/②/③/격상/제외 종류는 400(`SELF_DECIDE_*`), [상신만]=종전 PM 큐. '내 상신' "전결 종결 · M/D", 카드 참조 문서 줄("문서함 등재와는 별개"), 총정리 크론은 기존 집계(grade1+승인+decided_at)로 자동 포함(servertest 확인). ②문서함 첨부 — `gw_files` `docatt:<docId>:<n>` + 문서 `files` 메타·`att_seq` 카운터(`doc_att_put` 관리자·등재 본인, 확장자 9종·**base64 6MB(원본 4.5MB)**·**mime은 확장자 고정표 강제(클라 값 폐기)+매직바이트 검증 `BAD_MAGIC`** / `doc_att_get` docVisible 축·못 보면 404·type도 고정표만 / `doc_att_del` 관리자), **save 재구성이 files·att_seq를 관리자·비관리자 모두 서버 원본 고정**(`docFilesFix`)·관리자 저장도 직전 읽기 실패면 `PREV_READ_FAILED`, 수정 모달 첨부 영역(신규는 저장 후·선택 즉시 4.5MB 검사)·행 `📎 n`+[열기](Blob URL — 앱 고정표 mime만, 응답 type 무시). ③`doc_bulk_put`(관리자·cid 멱등 `docbulk:<cid>`·항목당 첨부 1~3·합계 5.5MB·항목 100·전량 검증 후 쓰기·즉시 등재·scope 미지정=비공개·cat 화이트리스트 밖=99). **적대 검증 8건 수리(9/4 2차)**: high1 mime 위장 XSS(고정표+매직) / med2 관리자 save 직전 읽기 실패 가드 / med3 한도 6MB·4.5MB·bulk 5.5MB / low4·7 att_seq 카운터+쓰기 후 재확인(`ATT_CONFLICT` 409) / low5 화이트리스트 hasOwnProperty / low6 att_parse `att_` 프리픽스 / low8 기안 종류에서 지시 제외+고아 힌트는 지시 id 형식만. ④**문서함 1층 12분류**(문서번호체계_개편안_20260904.md §2·§5 — 01 법인·등기 … 12 홍보·소개 + 99): 앱 `DOC_CATS`·`DOC_CAT_ORDER`·`#docCat`·`docCatOf`(정규식 `/JW-?(0[1-9]|1[0-2])(?!\d)/i`·구 텍스트 폴백 7종)·`DOC_SCOPE_CATS` / 서버 `DOC_CAT_SET`·`docCatOf`·`DOC_SCOPE_CATS`·`DOC_SETTINGS_DEFAULTS`(신설 전부 admin)·`DOC_CAT_LABEL`. 01 하드차단·데이터 마이그레이션 없음(CLAUDE.md의 JW-05-017·05-013 참조 4행은 문서 레포 이동과 동시에 바꿀 것 — 미변경). sw v315, servertest +76(204 통과), uismoke 동률 8종 추가(제외 목록·확장자·폴백 등급표·분류 키·라벨·설정 대상·정규식·mime 표). 상세 FEATURES §4.6 기안 절·§4.9 첨부/일괄, TESTLIST v315. **커밋·푸시 대기(코드만)**. git stash `partial self_decide`(죽은 에이전트 잔재)는 참고만 하고 pop 안 함 — 이 커밋에 전부 반영됐으니 배포 후 `git stash drop` 가능(PM 확인).
- **v314(9/4, 외부 세션) — 문서함 공개범위·등재 결재**(PM "문서함 일단 다 올리고 등재결재랑 공개범위 만들어라 내가 선택하게" + "비공개로 다 올리라고"): 문서 scope(전원/관리부+관리자/관리자만/직원 지정)+분류별 기본값(`settings:documents`, **초기 전부 관리자만=전부 비공개**, 권한관리 탭 "문서함 설정" 카드), 서버 하드차단(get 필터·save 재구성·PREV_READ_FAILED), 등재 결재(직원 등재=대기+서버 자동 상신 '문서함 등재' cid `docreg-<id>`·승인=등재/반려=반려·관리자=즉시 등재·gate none), 반려 재상신, `apprCreateItem` 공용 본체 분리, v313 [결재 상신] 제거, sw v314, servertest 56건 추가(기능 38 + 적대 검증 7건 재현 18). **적대 검증 7건 수리(9/4)**: 비관리자 기존 문서 scope·cat·no 원본 고정+모달 잠금(med1) / 상신 실패 고착 → 관리자 폴 반대 방향 복구+register_warn alert(med2) / 01 위장 소실 방지(med3) / 재상신=reg_n+1 명시 신호만(low4) / 삭제 updated_ts(low5) / 01 검사가 본인 예외보다 앞(low6) / 자가복구는 '대기'일 때만+승인 시 반려 잔존 정리(low7). 상세 FEATURES §4.9·TESTLIST v314. **커밋·푸시 대기(코드만)**. 배포 후 PM 할 일: col:documents 18건 `del:1,hidden_tmp:1` 해제(임시 숨김 — 코드는 소프트 삭제로만 취급, 지우지 않음) → 공개할 분류만 설정 카드에서 열기.
- **v311(9/3, 외부 세션) — 결재 3차 1차분: 업무별 결재 등급**(명세 Desktop\work\08_앱_시안\시안_결재등급_명세_20260903.md §2 전량 확정): kind→grade 스냅샷(①PM 전결/②결재라인/③대표 전결), settings:approvals 등급표+권한관리 탭 칩 UI, 서버 게이트(PM_ONLY·②단계 전환 to:'boss'·②PM 보류 불가·전결총정리=확인만), 큐 분리(대표=to boss+구건 / PM=to pm+구건), gw-appr-cron 신설(매월 1일 KST 08:00 전결 총정리 카드 — netlify.toml 선언·uismoke 등록). 구건·v308 운반일지 회귀 보존(간주 grade3). servertest 등급 매트릭스+크론 테스트 추가, uismoke 등급표 동률 검사 추가. **커밋·푸시 대기(코드만 작성)**.
- **v310(9/3, 외부 세션) — v308·v309 적대 검증 반영**: 대표 부재 시 결재 교착 해소(decide 게이트·클라 apprCanDecide에 boss_present 폴백), 할 일 알림 멱등키 항목+날짜(연기 시 재알림), 알림 본문에서 할 일 제목 제거(알림함 공용 노출), BOSS_ONLY를 ALREADY_DECIDED 뒤로(구 캐시 클라 409 건너뜀), 직원 홈 기한순 정렬, 날짜칸 change=저장·blur=재렌더, blobList prefix. 기존 실패 2건은 변경 전과 동일(uismoke auth↔data fam,hr · servertest 봇 ingest 스냅샷).
- **내 할 일 기한·알림(v309, 9/3, 외부 세션)**: mytasks 항목 due 필드 + 인라인 날짜칸(change만 청취 — v283 iOS 교훈) + D-day 배지·기한순 정렬, 신규 스케줄 함수 `gw-todo-cron`(KST 08:00, 회원별 오늘 기한 미완료 → 본인 푸시 1회, `todo:sent:<mid>` 멱등). 검증: 배포 후 함수 로그 members/items, 🔔 알림함 `todo-due-<날짜>`. 스케줄 함수는 외부 HTTP로 못 깨우므로 수동 검증은 Netlify 대시보드 실행 또는 gw-cron-kick에 target 분기 추가(미구현).
- **운반일지 대표 전용 결재(v308, 9/3, 외부 세션)**: PM 결정 "운반일지는 확인만" — approvals 항목 `to:'boss'`(워커·create 스탬프, 구건은 kind로 판정), 푸시=대표만(전 기기, 없으면 관리자 폴백), 클라 필터(apprCanDecide)로 비대표 관리자 뱃지·큐·auto 제외, 서버 decide `BOSS_ONLY`(보류 제외). 판정 규칙 3중(push.js isBoss·gw-data apprBossOnly·index apprBossOnly) — 바꾸면 동시에. 결재 3차 방향(PM 9/3): 업무별 등급 ①PM 전결(월·분기 총정리를 대표 확인용 상신) ②결재라인(담당→PM→대표) ③대표 전결(PM 확인만) — 시안 승인부터.
- **진척도 탭(v307, 9/3, 외부 세션 작업)**: 관리자 전용 사이드바 탭 + `gw-progress` 함수(세션·관리자 검증 후 `_lib/progress.json`의 HTML 반환) + Edge Function guard(netlify/edge-functions/guard.ts — 경로 정규화 후 /netlify/*·/tools/*·*.md·package*.json 404; toml `/netlify/*` force 재작성은 2중 방어. 9/3 실측: gw-auth.js·HANDOFF.md·tools/*.mjs가 200으로 열렸고 재작성만으론 /NETLIFY/ 변형이 뚫림) + `/w/*` noindex·no-referrer. 진척도 갱신 절차에 ④앱 레포 커밋(progress.json) 추가(00_진척도/진척도_빌드.js). 배포 후 실측: `/netlify/functions/_lib/progress.json`·`/NETLIFY/functions/_lib/progress.json`·`/HANDOFF.md`·`/tools/uismoke.mjs` GET이 404인지, `/w/wp.html`·`/data/duties_seed.json`·`/MANUAL.html`·`/index.html`은 200인지, 관리자 로그인 탭 표시·직원 미표시. 열린 결정: w/wd 벽지 전문(직원명 포함)을 축약판으로 바꿀지.
- 최근 배포 이력: v206(낙찰 자동오픈·conAmt 폴백) v207(통과선 등록등급 실계산) v208(계약 라우팅·중복가드) v209(A값 항상) v210(하한율 항상·≈) v211(대장 자동) v212~215(경화·낙관적 락)
- **화관법·올바로 자동화 가동(8/11~13, v243~256 + 크론)**: 화관법 제품 노선 평일 07:30 무인 제출(D+3, 금 D+3~5 — 8/17 접수번호 W6009-260070257/8 실측), 올바로 일일수집 매일 08:00(7일 창). 감시(0건=고장 신호, 1년 365일 — PM 확정) 1단계 watch.yml은 설계만 있고 **미구현**. '로컬 폴백 제거' 조건(무인 3일 연속)이 찼는데 폴백이 아직 돎 — 제거 대기
- **블로그AI 실전화(v261~v281, 8/14~16)**: 자동 실행(v261) → (A)/(B) 프롬프트 재설계(v265) → 도우미 폐기(v267, 실코드 백업 `jongwoon\.backups\jw-blog-helper-code-20260818\`, 폴더 556MB는 8/18 삭제) → 마커 파서 관용화(v270·272) → 초안 즉시 저장(v273) → 경고문 복사 제외(v274) → 외부 실측 반영: 장면 순서·캡션·1~2장 교차·길이 사진 연동(v275~277) → 역행 오탐 2회 수리·확정 목표 주입(v278~279) → 분량=구조 할당량·effort high(v280) → 생성 타임아웃 10분·폴링 12분(v281). 상세는 FEATURES §2.7. **현재 관문: PM 재생성 테스트(합격=4,500자+·고아 0·경고 0) → 첫 실게시(0건 — 저품질 회복 30일 타이머 미시작)**. 남은 것: PROMO_TEL 전화번호, 학습 시스템(승인글·메모 재료) 백로그
- **8/18 전수조사(실측)**: 수집 크론 전부 가동(입찰 07:48·백업 05:49 커밋 실측). 김과장 "화관법 제출 안된다" 건은 시스템 정상(위 접수번호)이라 개별 건 — 화면 문구 대기(NO_TEMPLATE=신규 차량·노선 조합이면 첫 1회 수동 제출이 사양). 올바로 PM 확인 5건 6일째 대기. KOITA 12일 정지 — 진척도에 "PC 배송 대기"와 "PC 주문할 것"이 공존(모순), PM 확인 필요. appdata 로컬 pull 완료. FEATURES 기준 v281로 갱신. 넷리파이 CLI 로그인 풀림(8/8분 만료 — 서버 기록 실측하려면 `netlify login` 재실행 필요)

## 5b. 진척도·체크리스트 표시 규격
PM이 진척도/체크리스트를 요청하면 **PROGRESS_FORMAT.md의 3단계 드릴다운 포맷**(가장 넓은 범위부터)으로 낸다. 좁은 범위부터 내밀면 반려된 실사고 있음.

## 6. 세션 시작 절차 (PM 프롬프트 한 줄이면 됨)
1. `claude/gw-backend` 체크아웃(또는 그 기반 브랜치 생성) — main 기반 금지
2. 이 파일 + CLAUDE.md + FEATURES.md 정독
3. 작업 후 §4 검증 → gw-backend push(배포됨을 인지) → 이 파일 §5 큐 갱신
